\documentclass{article} % For LaTeX2e
\usepackage{iclr2027_conference,times}

% Optional math commands from https://github.com/goodfeli/dlbook_notation.
\input{math_commands.tex}

\usepackage{hyperref}
\usepackage{url}

% --- added for this draft -------------------------------------------------
\usepackage{amsmath,amssymb,amsthm}
\usepackage{booktabs}
\usepackage{graphicx}
\usepackage{float}
\graphicspath{{images/}}
\usepackage{enumitem}

\theoremstyle{plain}
\newtheorem{theorem}{Theorem}
\newtheorem{proposition}[theorem]{Proposition}
\newtheorem{lemma}[theorem]{Lemma}
\theoremstyle{definition}
\newtheorem{definition}[theorem]{Definition}
\theoremstyle{remark}
\newtheorem{remark}[theorem]{Remark}

\usepackage{xcolor}                        % required by \zied below
\newcommand{\zied}[1]{\textcolor{blue}{#1}}

% -------------------------------------------------------------------------

\title{Diagnosing the implicit bias of gradient descent at the operator level}

\author{}

\newcommand{\RUNNING}{{\footnotesize\textsc{running}}}
\newcommand{\fix}{\marginpar{FIX}}
\newcommand{\new}{\marginpar{NEW}}

%\iclrfinalcopy % Uncomment for camera-ready version, but NOT for submission.
\begin{document}


\maketitle

\begin{abstract}
\end{abstract}

% =========================================================================
\section{Introduction}
\label{sec:intro}

A deep network trained by gradient descent does not merely reach \emph{a} minimiser of its
training loss; on an over-parameterised model it reaches a particular one, and which one it
reaches is a property of the optimiser rather than of the objective. This selection effect is
usually called the \emph{implicit bias} of the optimiser, and for factored linear models it is by
now well characterised: depth drives the solution toward low rank, and the effect strengthens as
layers are added \citep{gunasekar2017implicit,arora2019implicit,li2021towards}.

Almost all of that characterisation is obtained by looking at \emph{endpoints}. One trains to
convergence, inspects the solution, and repeats the experiment with a different depth, width, or
initialisation; the optimiser's preference is then inferred from how the endpoint moves. This is
an indirect instrument, for a reason that is structural rather than practical: the comparison has
no reference point. To say that gradient descent's step is biased, one would have to say what an
unbiased step would have been, and in weight space there is no canonical answer --- the loss
supplies a gradient, and the gradient is precisely the object whose bias is in question.

This paper supplies the missing reference by changing the space in which the question is asked.
For a bias-free network built from linear maps and positively homogeneous gates, the function
computed at an input $x$ factors through a matrix,
\begin{equation}
  f(x) = P(x)\,x ,
  \label{eq:fx}
\end{equation}
where $P(x)$ is the input--output Jacobian at $x$ (defined precisely in
Section~\ref{sec:setup}). In this \emph{operator} view the loss has a direct opinion about what
should happen: it asks the operator to move along $-\partial \ell/\partial P(x)$. That request is
well defined, it does not mention the parameterisation, and every optimiser can be measured
against it.

What a weight update actually does to the operator is a different matter. The update $\Delta W$ is
applied to the factors, and the factorisation converts it into an operator change through a linear
map $M$ that depends on the current weights. Two questions then become separable, and both are
measurable:

\begin{enumerate}[leftmargin=*,itemsep=2pt,topsep=3pt]
  \item \textbf{How much of what the loss asked for is achievable at all?} The range of $M$ may
        not contain the requested operator change. This is a property of the architecture and the
        current weights, and \emph{no} optimiser can do better than it.
  \item \textbf{Of the achievable part, how much does a given optimiser take?} Gradient descent,
        Adam, or any other rule produces some $\Delta W$; the operator change it induces can be
        compared in direction and magnitude with the best achievable one. This is a property of
        the update rule.
\end{enumerate}

Separating these two is the contribution of this paper. It requires being able to compute, at any
point in training, the weight update that realises a prescribed operator change as exactly as the
architecture permits and with the smallest possible norm --- what we call the \emph{reference
step}. We give solvers for it in two regimes: exactly, in closed form, for deep linear chains
(Section~\ref{sec:linear}), and as a least-squares problem on the step map for gated networks
(Section~\ref{sec:nonlinear}).

\paragraph{Contributions.}
\begin{enumerate}[label=\textbf{C\arabic*.},leftmargin=*,itemsep=3pt,topsep=3pt]
  \item \textbf{A reference step targeting the operator.} Given a prescribed change of the
        operator, we compute the minimum-norm weight update realising it, up to the reachable set.
        The construction is a preconditioned gradient step (Remark~\ref{rem:precond}) and we do not
        claim the family as new: on a deep linear network with whitened inputs it reduces exactly to
        the natural gradient of \citet{bernacchia2018exact}. What is new is the target. Prescribing
        an operator change constrains the full Jacobian at every input, where prescribing an output
        change --- as in the closest prior formulation, \citet{evans2026theory} --- constrains only
        its action along that input; the two separate precisely when the network is gated or the
        inputs are not whitened (Sections~\ref{sec:setup}, \ref{sec:nonlinear}).
  \item \textbf{A decomposition of the bias into an architectural and an algorithmic part.}
        Reachability measures what the architecture forbids; deflection measures what the
        optimiser gives up within what is allowed. The first is optimiser-independent, the second
        is scale-invariant and therefore independent of the learning rate
        (Section~\ref{sec:diagnostics}).
  \item \textbf{Measurements.} What the decomposition finds across model classes,
        initialisations, and optimisers (Sections~\ref{sec:linear-exp}, \ref{sec:nonlinear-exp}).
\end{enumerate}

% =========================================================================
\section{Setup: factored models, the operator, and the reference step}
\label{sec:setup}

This section defines every object used later, in order of dependence. Nothing here is specific to
a particular nonlinearity: the same definitions serve the deep linear networks of
Section~\ref{sec:linear} and the gated networks of Section~\ref{sec:nonlinear}, which is what
makes the diagnostics of Section~\ref{sec:diagnostics} comparable across model classes.

\subsection{Factored models}
\label{sec:models}

Throughout, a model has $L$ weight matrices $W_\ell \in \mathbb{R}^{n_\ell \times n_{\ell-1}}$ for
$\ell = 1,\dots,L$, and no bias terms. We write $W = (W_1,\dots,W_L)$ for the collection and
$\Delta W = (\Delta W_1,\dots,\Delta W_L)$ for an update. Between consecutive weight matrices sits
a \emph{gate} $D_\ell$, a matrix that may depend on the input. We consider five instances.

\begin{description}[leftmargin=*,itemsep=2pt,topsep=3pt]
  \item[Deep linear network (DLN).] $D_\ell = I$ for all $\ell$.
  \item[Fixed-gate linear network (FGLN).] $D_\ell$ is a fixed diagonal matrix, the same for every
        input. This interpolates between the linear and the gated case: the contexts below become
        input-dependent in form but not in value.
  \item[ReLU multilayer perceptron.] $D_\ell(x) = \mathrm{diag}\big(\mathbf{1}[z_\ell(x) > 0]\big)$,
        where $z_\ell(x)$ is the pre-activation at layer $\ell$.
  \item[Leaky ReLU multilayer perceptron.] $D_\ell(x) = \mathrm{diag}\big(\mathbf{1}[z_\ell(x)>0]
        + \alpha\,\mathbf{1}[z_\ell(x)\le 0]\big)$ with $\alpha = 0.1$. Every gate entry is
        strictly positive, so no unit can ever be dead. This is a control rather than a model of
        interest: it holds depth, width and the factorisation fixed while removing the possibility
        of unit death, which separates "the optimiser prunes units" from "the optimiser reduces
        how much the gate pattern depends on the input" (Section~\ref{sec:e8}). It is used where
        that distinction is at stake and omitted elsewhere.
  \item[CReLU multilayer perceptron.] The concatenated rectifier \citep{shang2016crelu} doubles the
        feature dimension,
        $c = [\,\mathrm{relu}(z)\,;\,\mathrm{relu}(-z)\,]$, which can be written $c = D(z)\,z$ with
        the rectangular gate
        \begin{equation}
          D(z) \;=\; \begin{bmatrix} \mathrm{diag}(\mathbf{1}[z>0]) \\[2pt]
                                     -\,\mathrm{diag}(\mathbf{1}[z<0]) \end{bmatrix}
          \in \mathbb{R}^{2d \times d},
          \qquad D(z)^\top D(z) = I .
          \label{eq:crelu}
        \end{equation}
        The isometry $D^\top D = I$ is what makes this architecture useful to us, for reasons
        given in Section~\ref{sec:lookslinear}.
\end{description}

All five gates are positively homogeneous of degree zero in the pre-activation, so the network is
positively homogeneous of degree one and \eqref{eq:fx} holds exactly rather than approximately.
Leaky ReLU is worth checking on this point rather than assuming it: $\max(z, \alpha z)$ is
positively homogeneous of degree one for any $\alpha$, so the gate is degree zero and the
factorisation is exact there too --- which is what makes it usable as a control at all.

Not every experiment uses all five. The deep linear and fixed-gate models are where the operator is
input-independent and the geometry can be drawn exactly (Section~\ref{sec:linear}); ReLU and CReLU
carry the main comparisons; leaky ReLU appears where dead units would otherwise confound the
reading. Each experiment states which it uses.

\subsection{The operator and its contexts}
\label{sec:contexts}

\begin{definition}[Operator]
The \emph{operator} at input $x$ is
\begin{equation}
  P(x) \;=\; W_L\, D_{L-1}(x)\, W_{L-1} \cdots D_1(x)\, W_1
  \;\in\; \mathbb{R}^{n_L \times n_0},
  \label{eq:P}
\end{equation}
so that $f(x) = P(x)\,x$. Where the gates are input-independent (DLN, FGLN) we write $P$ without
the argument, and $P$ is then shared by all inputs.
\end{definition}

For a gated network $P(x)$ is the Jacobian $\partial f/\partial x$ almost everywhere, so
\eqref{eq:P} is not a new object but the familiar one, written in a form that exposes how each
layer enters.

\begin{definition}[Contexts]
The \emph{right contexts} and \emph{left contexts} are defined by the recursions
\begin{equation}
  B_1 = I, \qquad B_{\ell+1} = D_\ell W_\ell B_\ell,
  \qquad\qquad
  A_L = I, \qquad A_\ell = A_{\ell+1} W_{\ell+1} D_\ell .
  \label{eq:contexts}
\end{equation}
\end{definition}

They are exactly the parts of the chain to the right and to the left of a given layer, and by
construction
\begin{equation}
  P(x) \;=\; A_\ell(x)\, W_\ell\, B_\ell(x)
  \qquad \text{for every } \ell = 1,\dots,L .
  \label{eq:AWB}
\end{equation}
Equation~\eqref{eq:AWB} is the only structural fact we use, and it holds verbatim for all four
model classes of Section~\ref{sec:models}; only the value of $D_\ell$ changes. This is why a
single implementation and a single set of diagnostics cover the whole family.

\subsection{What the loss asks of the operator}
\label{sec:target}

Let $\ell(\cdot)$ be a per-example loss and $\mathcal{L}$ the average over a batch. Because
$f(x) = P(x)x$, the loss can be differentiated with respect to the operator directly.

\begin{definition}[Operator gradient]
$G(x) \;:=\; \partial \ell / \partial P(x) \in \mathbb{R}^{n_L \times n_0}$.
\end{definition}

For the two losses used in this paper $G$ is rank one:
\begin{equation}
  G(x) = \big(f(x) - y\big)\,x^\top \quad\text{(squared loss)},
  \qquad
  G(x) = \big(\mathrm{softmax}(f(x)) - e_y\big)\,x^\top \quad\text{(cross-entropy)} .
  \label{eq:G}
\end{equation}
Combining \eqref{eq:AWB} with the chain rule gives the familiar backpropagated gradient in the
notation above,
\begin{equation}
  \frac{\partial \mathcal{L}}{\partial W_\ell}
  \;=\; \sum_x A_\ell(x)^\top\, G(x)\, B_\ell(x)^\top .
  \label{eq:backprop}
\end{equation}

\begin{definition}[Target]
Given a step size $\eta > 0$, the \emph{target} is the collection of operator changes the loss
requests, one per input:
\begin{equation}
  \Delta_\star \;:=\; \big(-\eta\, G(x)\big)_x .
  \label{eq:target}
\end{equation}
\end{definition}

\begin{remark}[The reference arm normalises this target, and what that changes]
\label{rem:normalise}
Definition~\ref{def:ref} and everything derived from it use $\Delta_\star$ exactly as written above,
and so does every \emph{diagnostic}: the alignment probe, reachability and deflection are all
computed against $(-\eta G(x))_x$. The reference \emph{arm}, when used to train, instead supplies
$\big(-\eta\, G(x)/\|G\|\big)_x$, normalising the residual so that $\eta$ sets the size of the
requested operator change directly rather than in proportion to the gradient.

This is deliberate --- it is what makes $\eta$ a scale-free target size, and it is why a single
$\eta$ transfers across depths at which the baselines' learning rates move by two to three orders
of magnitude (Section~\ref{sec:e2}) --- but it has a consequence that must be stated. The arm does
not slow down as it converges. At the end of a ReLU run its training loss is $1.4\cdot10^{-10}$
while its step is still of size $\eta$, roughly $10^{7}$ times the step the unnormalised rule would
take on that residual. Past interpolation it therefore continues to move at constant speed, driven
by a normalised residual that is dominated by whichever few samples are least well fit.

Several readings in this paper depend on that. The post-interpolation rank collapse of E9 occurs in
exactly this regime, and any value read at the end of the budget is read while this arm is still
moving and the other two have stopped. That asymmetry is why Part II reports every number at the
convergence phase rather than at the endpoint (Section~\ref{sec:common-setup}): gradient descent and Adam,
whose steps are proportional to the gradient, do come to rest, so for them the choice of window
changes little, while for the reference it changes the value and in one case the ordering.
\end{remark}

The target is defined without reference to the parameterisation. It is the statement ``move the
operator this way'', and it is the reference against which everything below is measured.

\subsection{The step map and the reference step}
\label{sec:stepmap}

An update $\Delta W$ changes the operator. To first order, substituting into \eqref{eq:P} and
collecting the terms linear in $\Delta W$,
\begin{equation}
  P\big(x;\, W + \Delta W\big) - P\big(x;\, W\big)
  \;=\; \sum_{\ell=1}^{L} A_\ell(x)\, \Delta W_\ell\, B_\ell(x) \;+\; O\!\left(\|\Delta W\|^2\right).
  \label{eq:firstorder}
\end{equation}

\begin{definition}[Step map]
The \emph{step map} is the linear operator sending a weight update to the collection of induced
operator changes,
\begin{equation}
  M(\Delta W) \;:=\; \Big( \textstyle\sum_{\ell} A_\ell(x)\, \Delta W_\ell\, B_\ell(x) \Big)_x ,
  \label{eq:M}
\end{equation}
with adjoint $\;(M^\top H)_\ell = \sum_x A_\ell(x)^\top H_x B_\ell(x)^\top$. We write $\Pi$ for the
orthogonal projector onto $\mathrm{range}(M)$.
\end{definition}

Comparing the adjoint with \eqref{eq:backprop} shows that gradient descent's weight step is
$\Delta W^{\mathrm{GD}} \propto M^\top \Delta_\star$. It is the adjoint applied to the target, not
the solution of any equation involving the target.

What we would like to ask for is exact: the smallest displacement whose \emph{realised} operator
change is the requested one,
\begin{equation}
  \min_{\Delta W} \; \sum_\ell \|\Delta W_\ell\|_F^2
  \quad \text{subject to} \quad
  P(x; W + \Delta W) - P(x; W) \;=\; \Delta_\star(x) \ \ \text{for every } x .
  \label{eq:exact}
\end{equation}
Note what is being minimised: the \emph{displacement} from the current weights, not the weights
themselves. Minimising $\sum_\ell\|W_\ell\|^2$ subject to realising a target is a different
problem --- the smallest factors realising it from scratch --- whose answer does not depend on
where the weights currently are, and whose classical solution is balanced. That is not the
question a training step asks.

Problem \eqref{eq:exact} is a polynomial system in $\Delta W$ and we do not solve it. Everything
below works with its linearisation, in which the constraint is replaced by $M(\Delta W) =
\Delta_\star$: the step map is the derivative of the left-hand side of \eqref{eq:exact} at
$\Delta W = 0$, so the two agree to first order. One further adjustment is needed. The gated
architectures do not in general admit every requested change --- $\Delta_\star$ need not lie in
$\mathrm{range}(M)$ --- so the best available request is its projection.

\begin{definition}[Reference step]
\label{def:ref}
The \emph{reference step} is
\begin{equation}
  \Delta W^{\mathrm{ref}}
  \;=\; \arg\min \; \|\Delta W\|_F
  \quad \text{subject to} \quad
  M(\Delta W) = \Pi\,\Delta_\star ,
  \qquad\text{equivalently}\qquad
  \Delta W^{\mathrm{ref}} = M^{+} \Delta_\star ,
  \label{eq:ref}
\end{equation}
where $M^{+}$ is the Moore--Penrose pseudo-inverse and $\|\cdot\|_F$ is the Euclidean norm on the
concatenated update.
\end{definition}

Minimum norm is not an arbitrary tie-break. The constraint in \eqref{eq:ref} fixes only the
first-order effect on the operator; any element of $\ker M$ may be added without changing it, and
such an addition moves the weights while leaving the function unchanged to first order. Requiring
the smallest update is what makes the reference a statement about the operator alone.

\begin{remark}[Relation to the conservation laws of gradient flow]
\label{rem:conserve}
$\ker M$ is the tangent space to the symmetry orbits of the parameterisation: for a linear chain,
$W_{\ell+1} \mapsto W_{\ell+1}G^{-1},\, W_\ell \mapsto G W_\ell$ leaves $P$ unchanged, and moving
along such an orbit is precisely an element of $\ker M$. A step lying in
$\mathrm{range}(M^\top) = (\ker M)^\perp$ therefore conserves the Noether charge associated with
that symmetry --- for a linear chain, the layer imbalance $W_{\ell+1}^\top W_{\ell+1} - W_\ell
W_\ell^\top$ --- which is the mechanism behind the conservation law of \citet{du2018algorithmic},
developed systematically by \citet{kunin2021neural} and characterised in full by
\citet{marcotte2023abide}. Both gradient descent, whose step is $M^\top \Delta_\star$, and the
reference, whose step is $M^{+}\Delta_\star = (M^\top M)^{+}M^\top \Delta_\star$, lie in
$\mathrm{range}(M^\top)$ by construction and so inherit it. We claim nothing new here; we record it
because it says what the reference step does \emph{not} do. It does not rebalance a network: an
unbalanced chain stays unbalanced under minimal displacement, and balancedness is a property of
minimising the weights themselves, which is a different problem. What is ours is the decomposition
as a \emph{measurement} --- which arms put mass in $\ker M$ and which do not --- reported in
Section~\ref{sec:e4}.
\end{remark}

\begin{remark}[The reference step is a preconditioned gradient step]
\label{rem:precond}
Since $M^{+} = (M^\top M)^{+} M^\top$ and $M^\top \Delta_\star = -\eta\,\nabla_W \mathcal{L}$ by
\eqref{eq:backprop}, Definition~\ref{def:ref} is equivalent to
\begin{equation}
  \Delta W^{\mathrm{ref}} \;=\; -\eta\,\big(M^\top M\big)^{+}\,\nabla_W \mathcal{L} .
  \label{eq:precond}
\end{equation}
The reference step is therefore a preconditioned gradient step, with the preconditioner given by
the pseudo-inverse of the Gram of the operator Jacobian. It belongs to the Gauss--Newton and
natural-gradient family, and we state this plainly rather than presenting it as a separate
construction. What distinguishes it is \emph{which} metric is pulled back. Gauss--Newton uses the
Jacobian of the \emph{outputs}; \eqref{eq:precond} uses the Jacobian of the \emph{operator}. These
coincide exactly for a deep linear network on whitened inputs --- there
$\sum_i x_i x_i^\top \propto I$ makes the two Gram matrices proportional, and the reference step
reduces to the exact natural gradient of \citet{bernacchia2018exact} --- and separate otherwise.
Measured on a depth-3 network, the cosine between the two steps is $1.000$ for deep linear with
whitened inputs, $0.881$ for deep linear with raw inputs, and $0.690$ and $0.388$ for ReLU with and
without whitening. The separation has a clear cause: prescribing $\Delta P(x)$ constrains the whole
Jacobian at each input, whereas prescribing $\Delta f(x)$ constrains only its action along $x$.
\end{remark}

\subsection{What we measure}
\label{sec:diagnostics}

\begin{definition}[Reachability]
\begin{equation}
  \alpha \;:=\; \frac{\|\Pi \Delta_\star\|}{\|\Delta_\star\|} \;\in\; [0,1].
\end{equation}
\end{definition}
$\alpha$ is the fraction of the requested operator change that the architecture admits at the
current weights. It depends on the model and the point, \emph{not} on the optimiser: no update
rule can realise more than $\alpha$. For a deep linear network with a single shared target,
$\alpha = 1$ generically; for gated networks $\alpha < 1$, and Section~\ref{sec:nonlinear-exp}
measures it.

\begin{definition}[Deflection]
Let $\Delta W_m$ be the update some optimiser would take from the current weights. We compare it
with the reference in each of the two spaces:
\begin{equation}
  \underbrace{\cos_{\mathrm{op}} \;=\; \cos\!\big(\,\overbrace{M(\Delta W_m)}^{\text{operator change it causes}},\
      \overbrace{\Pi \Delta_\star}^{\text{best available}}\,\big)}_{\text{operator space}},
  \qquad
  \underbrace{\cos_{\mathrm{w}} \;=\; \cos\!\big(\,\overbrace{\Delta W_m}^{\text{step it takes}},\
      \overbrace{\Delta W^{\mathrm{ref}}}^{\text{step it should have taken}}\,\big)}_{\text{weight space}},
\end{equation}
together with the corresponding norm ratios $\|M(\Delta W_m)\|/\|\Pi\Delta_\star\|$ and
$\|\Delta W_m\|/\|\Delta W^{\mathrm{ref}}\|$.
\end{definition}

In words, there are only three objects and each is the answer to a plain question.
$\Delta_\star$ is \emph{what the loss asked the operator to do}. $\Pi\Delta_\star$ is \emph{how much
of that the architecture can actually deliver} --- and when the model is over-parameterised
relative to the batch, $M$ is surjective, $\Pi\Delta_\star=\Delta_\star$, $\alpha=1$, and this
distinction disappears entirely. $\Delta W^{\mathrm{ref}}$ is \emph{the weight update that delivers
it}, at least norm.

\textbf{Why both spaces.} The two cosines can disagree, and the disagreement is exactly what the
reference makes visible. Any element of $\ker M$ may be added to a weight update without changing
the operator at all, so a high $\cos_{\mathrm{op}}$ beside a low $\cos_{\mathrm{w}}$ says an
optimiser reached roughly the right operator motion \emph{by a different route through weight
space}. Asking that question requires a reference that is itself a weight update, which is what
distinguishes this construction from a purely functional comparison.

Both are cosines, hence invariant to the scale of $\Delta W_m$ and therefore \textbf{independent of
the learning rate}; only the norm ratios carry one, which is why they are reported separately. This
matters for the evidential status of the comparison: a claim expressed in these quantities cannot
be answered by asserting that a baseline was mis-tuned.

\begin{remark}[Fidelity is not quality]
\label{rem:fidelity}
$\Delta_\star = -\eta\,\partial\mathcal{L}/\partial P$ is steepest descent \emph{in operator
space}. On an anisotropic loss, steepest descent is itself an indirect route to the minimiser: it
removes the steep directions first and then advances slowly along the flat ones. Consequently an
optimiser may score \emph{lower} on $\cos_{\mathrm{op}}$ and still reach the optimum \emph{sooner}
--- a method whose step approximates a whitened or second-order direction will do exactly that.
$\cos_{\mathrm{op}}$ quantifies how far the factorisation deflects the step the loss asked for. It
is not a ranking of optimisers and must not be read as one.
\end{remark}

\begin{remark}[$\eta$ is an operator step, not a learning rate]
The target \eqref{eq:target} asks the operator to move by $\eta\|G\|$, so $\eta$ reads directly as
a fraction of $\|P(x)\|$ and is comparable across depths and architectures. A learning rate is not:
the same numerical value means different things at different depths because the map from weights
to operator changes with depth.
\end{remark}

\begin{remark}[Gradient descent as a truncation]
\label{rem:krylov}
Solving \eqref{eq:ref} with a Krylov least-squares method started from zero, the first iterate is
proportional to $M^\top \Delta_\star$, which by \eqref{eq:backprop} is exactly the gradient descent
direction. The reference step is what the same solve returns on convergence. This is a convenient
implementation fact --- it means one routine produces both arms, and a truncation parameter
interpolates between them --- and we use it in Section~\ref{sec:s2}. We draw no further conclusion
from it.
\end{remark}

% =========================================================================
\section{What is already known}
\label{sec:sota}

\subsection{Dynamics and implicit bias in deep linear models}

The deep linear network is the standard setting in which implicit bias can be analysed rather than
only observed, because the function computed is a single matrix while the optimisation remains
non-convex. \citet{saxe2014exact} obtained exact solutions of the learning dynamics under gradient
flow from balanced and aligned initialisation with whitened inputs, exhibiting the sequential,
mode-by-mode learning that has since organised much of the field.
\citet{arora2018optimization} showed that over-parameterisation by depth acts as a preconditioner:
under gradient flow with a balanced initialisation, $W_{j+1}^\top W_{j+1} = W_j W_j^\top$, the
induced motion of the end-to-end matrix is
\begin{equation}
  \dot P \;=\; -\sum_{j=1}^{L}\big(PP^\top\big)^{\frac{L-j}{L}}\,G\,\big(P^\top P\big)^{\frac{j-1}{L}} ,
  \label{eq:arora}
\end{equation}
which on the singular values of $P$ reduces to a per-mode gain $L\,s^{2-2/L}$. Both hypotheses are
essential and neither is generic: \eqref{eq:arora} is a statement about gradient \emph{flow} from a
\emph{balanced} initialisation.

\citet{du2018algorithmic} showed that the layer-wise imbalance
$W_{\ell+1}^\top W_{\ell+1} - W_\ell W_\ell^\top$ is conserved under gradient flow for homogeneous
models, which is what makes balancedness a persistent property rather than an initial one, and
extends beyond the linear case. \citet{bah2022learning} identified the factor flow with a
Riemannian gradient flow on the manifold of fixed-rank matrices, and \citet{ablin2020deep} showed
that a deep \emph{orthogonally constrained} linear network is equivalent to a single layer, which
delimits how much of the depth effect survives when the factors are restricted.
\citet{wendin2026gradient} survey the gradient-flow equations for these models, and
\citet{cohen2024lectures} give a consolidated account of the area together with a list of
questions that remain open --- among them the behaviour of unbalanced initialisation beyond depth
two, which is the regime the reference step of Section~\ref{sec:stepmap} is designed to reach.

On the bias itself, \citet{gunasekar2017implicit} conjectured that gradient descent on a two-layer
factorisation with small initialisation and small steps converges to the minimum nuclear norm
solution, connecting the dynamics to the classical convex surrogate for rank
\citep{srebro2005maximum,recht2010guaranteed}. \citet{arora2019implicit} showed that depth
strengthens the tendency toward low rank in deep matrix factorisation, and
\citet{razin2020implicit} then showed that no norm --- nuclear or otherwise --- can explain the
bias in general, which redirected the field from characterising the endpoint by a penalty toward
characterising the trajectory. \citet{li2021towards} showed that the flow from infinitesimal
initialisation performs a greedy low-rank search, and \citet{jacot2021saddle} described the
saddle-to-saddle dynamics in which the effective rank increases by one at each saddle escape.
\citet{tarmoun2021understanding}, \citet{min2021explicit} and \citet{kunin2024get} analysed how the
initialisation --- in particular its imbalance --- controls both the convergence rate and the
resulting bias, showing that unbalanced initialisation is not a perturbation of the balanced theory
but a qualitatively different regime.

\subsection{Beyond the linear case}

For nonlinear networks the picture is less complete. \citet{timor2023implicit} established
regularisation toward rank minimisation in ReLU networks under stated conditions, and
\citet{jacot2023implicit} introduced a notion of rank for nonlinear functions under which large
depth induces a bias toward low rank. \citet{bantzis2025saddle} extended the saddle-escape analysis
to deep ReLU networks, finding a low-rank bias at the first escape, and \citet{rawal2026saddle} give
a theory of saddle escape in deep nonlinear networks. \citet{gatedlinear2022implicit} analysed
generalized gated linear networks, which is the closest published setting to the FGLN model of
Section~\ref{sec:models}. \citet{huh2021lowrank} documented the low-rank simplicity bias
empirically across architectures. A separate line studies the average gradient outer product as the
mechanism of feature learning \citep{radhakrishnan2024mechanism,beaglehole2024agop}.

Two further results bear directly on the objects we measure. \citet{pennington2017resurrecting}
showed that ReLU networks cannot achieve dynamical isometry --- the input--output Jacobian cannot
have a well-conditioned spectrum at depth --- which is why the reachability $\alpha$ of
Section~\ref{sec:diagnostics} cannot be expected to equal one for ReLU, and why a nonlinear
architecture that \emph{is} conditioned by construction (Section~\ref{sec:lookslinear}) is needed as
a control. \citet{wang2016extended} proposed the extended data Jacobian matrix as a diagnostic,
which is an operator-level measurement in our sense, though used for model comparison rather than
for scoring update rules. \citet{haas2026why} analyse how the Jacobian spectra of deep networks
separate with depth, and establish a conservation law for the gate-absorbed imbalance.

\subsection{Geometry-aware and second-order optimisation}

The reference step of Definition~\ref{def:ref} is a preconditioned gradient step
(Remark~\ref{rem:precond}), so it belongs squarely to this literature and we position it there
rather than apart from it. \citet{bernacchia2018exact} computed the exact natural gradient for deep
linear networks and observed that it annihilates precisely the depth-induced preconditioner in
\eqref{eq:arora}. That is the closest relation, and it is an identity rather than an analogy: on a
deep linear network with whitened inputs the reference step \emph{is} their natural gradient, since
the operator and output Gram matrices are then proportional. It separates from Gauss--Newton and
from natural gradient once the inputs are not whitened or the gates are input-dependent, because
prescribing $\Delta P(x)$ constrains the entire Jacobian at $x$ while prescribing $\Delta f(x)$
constrains only its action along $x$.

\citet{evans2026theory} give the closest published formulation of the underlying problem: they
derive minimal-norm weight perturbations achieving a specified change in \emph{output}, exactly for
a single layer, and compare against Lipschitz-based bounds for the multi-layer case; their purpose
is robustness certification rather than optimiser analysis. The difference from our setting is the
target: an output change constrains the network along one input, an operator change constrains its
action on every input at once. \citet{frerix2018proximal} recast
backpropagation as a sequence of proximal steps, and \citet{dufortlabbe2026layerwise} develop a
layerwise LQR view of geometry-aware optimisation. The distinction of purpose is worth stating
once: all of these are optimisation methods proposed to train faster, whereas we use the algebra as
a \emph{measuring instrument}, and Remark~\ref{rem:fidelity} explains why the two goals must not be
conflated.

\subsection{The gap}

Across this literature the bias is inferred from endpoints, and its architectural and algorithmic
components are not separated. Statements of the form ``depth strengthens the low-rank bias'' are
established by varying depth and observing the endpoint; they do not distinguish a change in what
the architecture \emph{permits} from a change in what the optimiser \emph{does} within what is
permitted. The reference step makes that separation computable, and the two diagnostics of
Section~\ref{sec:diagnostics} report the two halves.

\begin{table}[t]
\caption{Results we rely on or measure against, with the hypotheses each actually requires.}
\label{tab:sota}
\begin{center}
\small
\begin{tabular}{p{4.0cm}p{1.6cm}p{5.0cm}p{2.2cm}}
\toprule
\textbf{Result} & \textbf{Model} & \textbf{Hypotheses required} & \textbf{Source} \\
\midrule
Exact dynamics; modes learned largest-first & DLN &
gradient flow; balanced \emph{and} aligned init; whitened inputs & \citet{saxe2014exact} \\
Induced step \eqref{eq:arora}; per-mode gain $L s^{2-2/L}$ & DLN &
gradient \emph{flow}; balanced init & \citet{arora2018optimization} \\
Layer imbalance conserved & homogeneous &
gradient flow; homogeneity & \citet{du2018algorithmic} \\
Factor flow $=$ Riemannian flow on fixed-rank manifold & DLN & --- & \citet{bah2022learning} \\
Deep orthogonal linear $\equiv$ one layer & DLN & orthogonality constraint & \citet{ablin2020deep} \\
Bias toward minimum nuclear norm & 2-layer & small init and step; \emph{conjecture} & \citet{gunasekar2017implicit} \\
Depth strengthens the low-rank bias & deep MF & matrix sensing / completion & \citet{arora2019implicit} \\
No norm explains the bias & deep MF & specific instances & \citet{razin2020implicit} \\
Flow $\equiv$ greedy low-rank learning & 2-layer & infinitesimal init & \citet{li2021towards} \\
Saddle-to-saddle; rank $+1$ per escape & DLN & small init & \citet{jacot2021saddle} \\
Rank minimisation in ReLU networks & ReLU & stated conditions & \citet{timor2023implicit} \\
Large depth induces low nonlinear rank & nonlinear & notion of rank defined therein & \citet{jacot2023implicit} \\
ReLU cannot achieve dynamical isometry & ReLU & random init, large width & \citet{pennington2017resurrecting} \\
Exact natural gradient; cancels the depth preconditioner & DLN & invertible Fisher & \citet{bernacchia2018exact} \\
\bottomrule
\end{tabular}
\end{center}
\end{table}

% =========================================================================
\section{Part I --- Deep linear networks}
\label{sec:linear}

In a deep linear network the gates are the identity, so $P = W_L \cdots W_1$ is a single matrix
shared by every input. The target reduces to one matrix, $\Delta_\star = -\eta\,
\partial\mathcal{L}/\partial P$, and the step map \eqref{eq:M} carries weight updates into the
space of $n_L \times n_0$ matrices.

This is the regime in which the framework can be \emph{seen} rather than only measured. With a
two-dimensional operator the trajectory of $P$ under any optimiser can be drawn exactly, against
a loss whose contours are exact ellipses, and every difference between the paths is attributable
to how each rule converts a weight step into an operator step --- since all of them descend the
same quadratic in $P$. That is the subject of this part. The substantive comparisons between
optimisers are deferred to Part II, for the reason given in Section~\ref{sec:whatchanges}: a deep
linear network with enough samples pins its single shared $P$, so all methods that fit the data
must agree, and there is nothing at the endpoint to distinguish.

\subsection{Experiments}
\label{sec:linear-exp}

Each experiment below states its motivation, its exact configuration, the quantity measured with
its formula, its current status, the numbers obtained, and what may and may not be concluded from
them. Status is one of \textsc{done}, \textsc{partial} or \textsc{todo}.

\subsubsection{E2 --- Operator-space trajectories}
\label{sec:e2}

\paragraph{Motivation and question.}
The diagnostics of Section~\ref{sec:diagnostics} are per-step scalars. \emph{What do the
trajectories themselves look like, and is the separation between rules visible as geometry rather
than only as a summary statistic?} A deep linear chain answers it exactly: the operator is a single
$1\times2$ matrix, so the path can be drawn without projection, and the loss is exactly quadratic,
so the geometry is not in question.

\paragraph{Why operator space and not weight space.}
This is not a presentational choice. A model whose \emph{weight} space is two-dimensional has a
rank-one step map $M = [\,w_2\ \ w_1\,]$, whence $M^{+} \propto M^\top$ and gradient descent
coincides with the reference \emph{exactly} --- measured at $\cos = 1.0000000000$. No
parameterisation effect can appear in such a picture. With $L$ layers feeding a two-dimensional
operator, $M$ has rank two and the rules separate. Every difference visible below is therefore
attributable to how each rule converts a weight step into an operator step, since all of them
descend the same quadratic in $P$.

\paragraph{Setup.}
A bias-free linear chain of depth $L$ and width $32$ mapping two features to one output, so
$P \in \mathbb{R}^{1\times2}$: $X \in \mathbb{R}^{128 \times 2}$ with feature scales $(1.0, 0.35)$
so the loss is anisotropic, $y = Xw$ with $w$ drawn once, float64, $300$ steps. Depths
$L \in \{2, 32\}$ and initialisations $\{$Xavier, orthogonal$\}$. Eight optimisers: gradient
descent, Adam, the reference, Muon, Shampoo, SOAP, K-FAC and heavy ball.

Two configuration notes. He initialisation is omitted: applied to a \emph{linear} chain it assumes
a rectifier halves the variance and there is nothing to halve it, so the gain is $2$ per layer and
the operator reaches $\|P\|_\infty = 1.1\times10^{4}$ at $L=32$, from which no optimiser in the grid
recovers. Looks-linear initialisation is also omitted: the $[\,O \mid -O\,]$ construction requires
CReLU's doubled feature dimension (Section~\ref{sec:lookslinear}), so on a linear chain it reduces
to the orthogonal case and contributes no separate panel.

The reference here is \eqref{eq:ref} solved to convergence --- a damped minimum-norm solve of the
same minimum-displacement problem --- rather than the Krylov truncation at $k = 50$ used as the
practical solver in Part II. On a deep linear chain the step map is largely null, measured at $83\%$
nullity at $L=6$, width $8$, and a truncated solve there reflects the solver's iteration budget
rather than the rule.

Each arm's step size is swept over a grid spanning $10^{-5}$ to $10^{-1}$ and the run drawn is the
one that descends most directly among those reaching within a factor of two of that arm's best
final loss. Selecting purely by lowest loss admits step sizes that overshoot and oscillate across
the optimum: such a run lands low, but its path is a zigzag that describes the tuning rather than
the rule.

\paragraph{Measurement.}
For the figure, the operator path itself. Separately, on a depth series at width $8$ with five
seeds, the realised operator change $\Delta P$ and its alignment with the request,
\begin{equation}
  \mathrm{align} \;:=\; \cos\!\Big(\Delta P,\ -\tfrac{\partial \mathcal{L}}{\partial P}\Big),
  \label{eq:align}
\end{equation}
from which we report the fraction of steps on which alignment is negative --- on which the step
moved the operator \emph{against} the direction the loss asked for.

\paragraph{Status.} \textsc{done}.

\paragraph{Results.}

\begin{figure}[H]
\centering
\includegraphics[width=\textwidth]{fig_traj_linear.pdf}
\caption{Operator-space trajectories of eight optimisers on a deep linear chain, at $L = 2$ (top)
and $L = 32$ (bottom), under Xavier (left) and orthogonal (right) initialisation. Dots mark
initialisation, the star marks the ordinary least squares solution, grey curves are exact loss
contours, and axes are scaled per panel. All eight converge in all four panels.}
\label{fig:trajlinear}
\end{figure}

Negative-alignment rates on the depth series, mean over five seeds with the per-seed range in
brackets:

\begin{center}\small
\begin{tabular}{lccc}
\toprule
depth & gradient descent & Adam & reference \\
\midrule
$L=2$  & $0.3\%$ $[0,2]$ & $9.3\%$ $[0,22]$  & $0.0\%$ \\
$L=8$  & $1.0\%$ $[0,3]$ & $33.0\%$ $[28,37]$ & $0.0\%$ \\
$L=32$ & $0.0\%$ $[0,0]$ & $26.9\%$ $[15,37]$ & $0.1\%$ \\
\bottomrule
\end{tabular}
\end{center}

\paragraph{Conclusions.}
\emph{The rules separate geometrically, and the separation is not a difference in destination.} All
eight reach the least squares solution in all four panels; what differs is the route. The reference,
gradient descent and K-FAC descend along nearly the same arc --- which is the visual form of K-FAC's
predicted closeness to the projection (Section~\ref{sec:sota}) --- while SOAP and heavy ball take
wide excursions transverse to it before turning in, and Muon takes a distinct intermediate path.
Since every arm minimises the same quadratic in $P$, those excursions are produced entirely by how
each rule maps a weight step into an operator step.

\emph{Depth widens the spread.} At $L=2$ the arms are bunched; at $L=32$ they fan out, and the
step sizes that survive selection fall by two to three orders of magnitude for the elementwise
methods (Adam from $10^{-3}$ to $10^{-5}$ at Xavier) while the reference's target size is unchanged
at $\eta = 0.3$ across both depths and both initialisations. The reference has no step size to
retune because its scale is set by the requested operator change rather than by the curvature of the
parameterisation.

\emph{Alignment is fidelity, not optimisation quality.} Adam moves the operator against the
requested direction on a substantial fraction of steps at depth, in every seed, while gradient
descent and the reference essentially never do. Yet at $L=32$ Adam reaches
$\|P - P^\star\| = 0.0000$ while the reference reaches $0.1437$: the worse-aligned method is the
faster one. Remark~\ref{rem:fidelity} states this distinction and it is enforced throughout the
paper.

% =========================================================================
\section{Part II --- Gated networks}
\label{sec:nonlinear}

\subsection{What changes}
\label{sec:whatchanges}

Three things change at once when the gates become input-dependent, and it is worth separating
them.

\begin{enumerate}[leftmargin=*,itemsep=2pt,topsep=3pt]
  \item \textbf{The operator is no longer shared.} Each input has its own $P(x)$, and the target
        \eqref{eq:target} asks for a different operator change at each one. A single weight update
        must serve all of these requests simultaneously.
  \item \textbf{The requests are not jointly realisable.} Since one $\Delta W$ produces the whole
        collection $M(\Delta W)$, and the collection has far more degrees of freedom than
        $\Delta W$, in general $\Delta_\star \notin \mathrm{range}(M)$ and $\alpha < 1$. The
        reference step realises the projection $\Pi \Delta_\star$, which is the most any optimiser
        can do.
  \item \textbf{Each sample constrains only a rank-one slice of its own operator.} Fitting the
        data requires $P(x_i) x_i = y_i$, which fixes the action of $P(x_i)$ along $x_i$ and leaves
        the remaining directions free. This is why gated networks can exhibit genuine differences
        between optimisers at the same training loss, while a deep linear network with enough
        samples cannot: there the single shared $P$ is pinned by the data, and all methods that fit
        must agree.
\end{enumerate}

Point 3 is the reason the endpoint comparisons of Section~\ref{sec:nonlinear-exp} are posed for
gated networks and not for deep linear ones.

\subsection{CReLU and looks-linear initialisation: a conditioned nonlinear control}
\label{sec:lookslinear}

The ReLU network confounds two effects that we wish to separate: it is nonlinear, and at depth it
is ill-conditioned, necessarily so \citep{pennington2017resurrecting, haas2026why}. To vary gating and
conditioning independently we need a network that is genuinely nonlinear yet perfectly conditioned
by construction. The concatenated rectifier of \eqref{eq:crelu}, introduced by
\citet{shang2016crelu} to exploit the opposite-phase filter pairs observed in trained
convolutional networks, provides one.

Write the layer weight in block form $W = [\,O \mid -O\,]$ with $O$ orthogonal --- the
\emph{looks-linear} initialisation of \citet{balduzzi2017shattered}, introduced there to keep
gradients from shattering with depth by making the network exactly linear at initialisation. Then for every $z$,
\begin{equation}
  W D(z) \;=\; O\,\mathrm{diag}(\mathbf{1}[z>0]) \;+\; O\,\mathrm{diag}(\mathbf{1}[z<0]) \;=\; O ,
\end{equation}
because the two indicator masks are complementary. Hence $P(x) = O_L \cdots O_1$ is orthogonal, at
every input and at \emph{any} depth: all singular values equal one, and the operator is identical
across inputs at initialisation. The network is nonetheless a genuine CReLU network --- the gates
are real, they differ between inputs as soon as training moves the weights away from the
looks-linear point, and no reparameterisation makes it linear.

This gives the control the rest of Part II requires: CReLU with looks-linear initialisation is
nonlinear and perfectly conditioned; ReLU with a standard random initialisation is nonlinear and
ill-conditioned at depth; the deep linear network of Part I is linear. Differences attributed to
gating can then be distinguished from differences attributable to conditioning.

\subsection{Solver: least squares on the step map}
\label{sec:s2}

For gated networks we solve \eqref{eq:ref} directly as a least-squares problem,
\begin{equation}
  \Delta W^{\mathrm{ref}} \;=\; \arg\min_{\Delta W} \; \big\|M(\Delta W) - \Delta_\star\big\|^2 ,
  \qquad \text{minimum-norm solution},
\end{equation}
using LSQR \citep{paige1982lsqr}, which requires only products with $M$ and $M^\top$ and never
forms the matrix. By \eqref{eq:M} and its adjoint, both products are batched matrix
multiplications over layers and inputs, so the solve costs $O(k)$ applications of the forward and
backward context products for $k$ iterations. By Remark~\ref{rem:krylov}, $k = 1$ returns the
gradient descent direction and large $k$ returns the reference step, so a single routine produces
both arms.

\paragraph{A numerical caveat that must not be skipped.}
LSQR's iterates are built inside the Krylov space
$\mathcal{K}_k(M^\top M, M^\top \Delta_\star) \subseteq \mathrm{range}(M^\top)$, and that
containment is exactly why the limit is the \emph{minimum-norm} least-squares solution. In finite
precision it does not hold: the Golub--Kahan bidiagonalisation loses orthogonality, the iterates
leave that subspace, and a component in $\ker M$ grows without bound. Measured on a depth-$2$
CReLU chain of width $128$, $\|\Delta W\|$ is $0.85$ at $k = 200$ and $9.3\cdot 10^{13}$ at
$k = 3000$, with $\|M(\Delta W)\| / \|\Delta W\| = 2\cdot10^{-14}$: the growth is weight motion
that produces essentially no operator motion. Running to a fixed large $k$ therefore returns a
ruined step, with no error raised.

The obvious guards do not help. The quantity $\bar\phi$ maintained internally by the recursion
satisfies $\bar\phi \leftarrow s\,\bar\phi$ with $s \le 1$, so it is monotone by construction and
can never signal that anything has gone wrong. The least-squares residual
$\|M(\Delta W) - \Delta_\star\|$ converges to a nonzero value that is not known in advance, so
there is no threshold to test it against --- and, being built from $M$, it is blind to motion
inside $\ker M$ by definition. So is the normal-equation residual
$\|M^\top(M(\Delta W) - \Delta_\star)\|$: adding $10^{6}$ times a null direction to a converged
iterate leaves both unchanged to four significant figures.

What we rely on is therefore not detection. The normal-equation residual is the optimality
condition for $\min\|M(\Delta W) - \Delta_\star\|$ and does converge to zero, so unlike the plain
residual it can be thresholded; and because it is not monotone, we retain the \emph{best} iterate
under it rather than the last. Drift cannot improve that quantity, so a drifted iterate is never
the one returned. Its attained value is recorded alongside every measurement, and reported in
Appendix~\ref{app:numerics}.

Two consequences are worth stating because they shape the experiments. Where the reference is
wanted to converge, the solve is Tikhonov-damped: $\ker([M;\lambda I]) = \{0\}$, so there is no
null direction left to drift into, which is a structural fix rather than a tolerance. And where
the truncation $k$ is itself the object of study (Section~\ref{sec:e7}), the solve is deliberately
undamped, so solution quality is \emph{not} monotone in $k$ --- beyond a few hundred iterations it
degrades. All results reported here use $k \le 200$, below that threshold.

\subsection{Experiments}
\label{sec:nonlinear-exp}

Every experiment below states its motivation and the question it answers, its exact configuration,
the quantity measured with its formula, its status, the numbers obtained, and what may and may not
be concluded. Status is \textsc{done}, \textsc{partial} (running, numbers below are from the cells
completed so far) or \textsc{queued}.

\subsubsection{Common configuration}
\label{sec:common-setup}

Unless an experiment states otherwise, all of Part II uses the following.

\paragraph{Task.} MNIST-1D \citep{greydanus2020mnist1d}: $40$-dimensional inputs, $10$ classes,
$4000$ training, $1000$ validation and $1000$ test points, batch size $100$, cross-entropy loss.
The task is small enough that every arm can be run to interpolation on a full hyper-parameter grid
with several seeds, which is what makes the comparisons below matched rather than budget-limited.

\paragraph{Model.} Bias-free chain of depth $L = 8$ and width $128$, He initialisation. The main
grid is ReLU and CReLU, the two gated architectures that carry the comparison; leaky ReLU
($\alpha = 0.1$) is added wherever unit death would otherwise confound the reading, and each
experiment states whether it is included. Deep linear is not part of this grid, for the reason in
Section~\ref{sec:whatchanges}: its single shared operator is pinned by the data, so all methods
that fit must agree. Bias-freedom is what makes
$f(x) = P(x)x$ exact, so the operator is the object the model actually computes rather than an
approximation to it.

\paragraph{Arms.} \emph{gradient descent} and \emph{Adam}, each over a learning-rate grid, and the
\emph{reference} step of Section~\ref{sec:s2} over a grid of target sizes $\eta$ at truncation
$k = 50$. Training runs for $12000$ steps, which is past interpolation for every arm.

\paragraph{Selection and scoring: the convergence phase.} \label{par:convphase} The $12000$-step
budget is chosen so that every arm reaches interpolation, which means every arm also spends time
\emph{past} it, and the arms do not arrive together: on ReLU the reference interpolates at step
$750$ and Adam at step $8083$, a factor of ten apart. A statistic read at a common wall-clock
endpoint therefore compares one arm shortly after it has fit the data against another that has been
sitting at zero training loss for ten thousand steps, and what happens in that interval is not the
same for all of them --- Section~\ref{sec:e9} shows one arm whose operator degrades there while the
others come to rest. Reading at the endpoint would attribute that degradation to the method rather
than to the length of its post-interpolation tail.

Every number in Part II is therefore reported at the \emph{convergence phase}, defined per run as
the first $8$ probes starting from the earliest probe at which that run's \emph{training} accuracy
reaches $0.999$. The anchor is each arm's own arrival at interpolation, so arms are compared at a
matched point in their own trajectory rather than at a matched step count. The trigger uses
training accuracy only --- no test quantity enters the choice of window --- so the estimator is
blind to the statistic it is used to report, and hyper-parameters are selected by the same estimator
that scores them. Runs that never reach the threshold are excluded and reported as such.

This replaces the last-quarter mean used in earlier drafts of this work, and the replacement is not
cosmetic: where an arm's operator decays after interpolation, the two windows disagree in the
\emph{ordering} of the arms, not merely in the third digit (Section~\ref{sec:e9}). Choosing the
window consistently matters for a second reason as well. These trajectories oscillate after
interpolation, so selecting a run at the peak of its validation curve while scoring it by a stable
statistic --- or the reverse --- introduces a bias of the same order as the differences being
measured. Figure~\ref{fig:selection} plots that gap against the post-interpolation swing of each
run. Selection and scoring use one estimator throughout.

\begin{figure}[H]
\centering
\includegraphics[width=0.78\textwidth]{selection_bias.pdf}
\caption{Left: three estimators of the same runs --- the peak of the validation curve (pale), the
convergence-phase mean used throughout Part II (solid), and the last-quarter mean (hatched). Right:
the test-accuracy swing over the second half of training, which is what makes them differ. The
three disagree by an amount comparable to the between-arm differences of Section~\ref{sec:e5},
which is why selection and scoring never mix them, and the size of the swing is also what makes the
\emph{position} of the window matter.}
\label{fig:selection}
\end{figure}

\paragraph{Diagnostics.} The alignment probe of Section~\ref{sec:diagnostics} runs every $2000$
steps on a fixed batch of $64$ training points, in float64, with the reference solved to $k = 3000$
with relative Tikhonov damping $10^{-7}$. Solve quality is recorded at every probe as the relative
normal-equation residual
\begin{equation}
  \mathrm{ne} \;:=\; \frac{\big\|M^\top\big(M(\Delta W^{\mathrm{ref}}) - \Delta_\star\big)\big\|}
                          {\big\|M^\top \Delta_\star\big\|},
  \label{eq:ne}
\end{equation}
and probes with $\mathrm{ne} > 10^{-2}$ are discarded. The normal-equation residual is the right
stopping test here: the ordinary residual $\|M\Delta W - \Delta_\star\|$ is blind to motion inside
$\ker M$, which is exactly the direction an undamped iteration drifts in.

\paragraph{Seeds.} Five seeds for the main grid, three for the sweeps, stated per experiment.
Reported uncertainties are standard deviations across seeds.

\paragraph{Orientation: the whole trajectory, before any single number is taken from it.}
The experiments below each extract one statistic from these runs. Figure~\ref{fig:dynamics} shows
the runs themselves --- every tracked quantity against training step, for all three arms at their
best configuration, with a band at one standard deviation across seeds --- so that the statistics
can be read against the dynamics that produced them. Four features of it are used repeatedly later
and are worth naming here.

First, the arms reach interpolation at very different times: the dashed markers sit at step $800$
for the reference and $7250$ for Adam on ReLU, a factor of nine. Second, \emph{test loss rises for
all three arms} from an early minimum, while test accuracy is flat or still slowly improving over
the same interval --- the networks are bias-free and positively homogeneous, so the logit scale
grows without bound and the cross-entropy on misclassified points grows with it. Test loss is
therefore not a usable model-selection signal here, and every accuracy in Part II is an accuracy
rather than a loss for that reason. Third, the operator-rank panel shows the reference rising above
its initialisation, holding, and then collapsing from around step $4000$ --- the phenomenon
Section~\ref{sec:e9} measures. Fourth, gradient descent is visibly static after step $1000$ in
every panel, which is why the choice of measurement window barely affects it.

\begin{figure}[H]
\centering
\includegraphics[width=\textwidth]{dynamics_relu.pdf}
\caption{Every tracked quantity against training step on ReLU, for each arm at its best
configuration (given in the legend), averaged over five seeds with a band at one standard
deviation. Vertical dashed lines mark where each arm first reaches training accuracy $0.999$; to
the right of an arm's line, that arm is training past interpolation. The bottom-right panel is an
alignment diagnostic and runs every $2000$ steps rather than every $250$, hence the markers.
The CReLU and leaky-ReLU versions are Figures~\ref{fig:dynamics-crelu}
and~\ref{fig:dynamics-leaky}.}
\label{fig:dynamics}
\end{figure}

% ---------------------------------------------------------------------------
\subsubsection{E3 --- How much of the request is reachable at all}
\label{sec:e3}

\paragraph{Motivation and question.}
Reachability is a ceiling: a property of the architecture and the current weights that no optimiser
can exceed. \emph{Is the gap between optimisers a gap in what they deliver, or in what the
architecture permits?} The question must be settled before any optimiser is scored, because a low
score against a low ceiling means something entirely different from a low score against a high one.

\paragraph{Setup.} The common configuration, probed along the trajectory of each arm.

\paragraph{Measurement.}
$\alpha := \|\Pi \Delta_\star\| / \|\Delta_\star\| \in [0,1]$, the fraction of the requested
operator change that lies in $\mathrm{range}(M)$. Being a projection ratio, $\alpha > 1$ is
impossible and serves as an independent check on the solve.

\paragraph{Status.} \textsc{done}.

\paragraph{Results.}
At the convergence phase, $\alpha = 1.000$ for gradient descent in all three gated architectures and
$0.993$--$1.000$ for the reference, so for those two the requested change is reachable to within the
solve's own precision. Adam reads $0.770$ on ReLU and $0.993$--$1.000$ on the other two.

\paragraph{The low Adam value is the solver, not the architecture.} It would be easy to read
$\alpha = 0.770$ as ``a fifth of what the loss asks of Adam's operator is unreachable'', and an
earlier version of this work did. That reading does not survive checking. $\alpha$ is computed from
a truncated solve, and the truncation \emph{under}-estimates it: a projection resolved to fewer
iterations recovers less of $\Pi\Delta_\star$ and so reports a smaller norm ratio. Re-solving at an
Adam checkpoint with the budget raised gives

\begin{center}\small
\begin{tabular}{lccc}
\toprule
LSQR iterations & $3000$ & $10000$ & $30000$ \\
\midrule
$\mathrm{ne}$ & $8.4\cdot10^{-2}$ & $3.7\cdot10^{-2}$ & $1.5\cdot10^{-2}$ \\
$\alpha$      & $0.761$ & $0.839$ & $0.870$ \\
gradient descent's $\cos_{op}$ at the same weights & $0.133$ & $0.121$ & $0.116$ \\
\bottomrule
\end{tabular}
\end{center}

\noindent$\alpha$ rises monotonically with the budget and has not converged even at $30000$
iterations, where $\mathrm{ne}$ is still above the $10^{-2}$ gate. Adam's true $\alpha$ on ReLU is
therefore at least $0.87$ and the measured $0.770$ is a floor. What the table does establish is
real, and it is about conditioning rather than reachability: the same solve that converges to
$\mathrm{ne} \approx 10^{-5}$ in $3000$ iterations at the reference's weights needs more than ten
times that at Adam's, which is consistent with the large $\ker M$ fraction of E4. Note also that
the cosine in the last row moves by $0.017$ across a budget that moves $\alpha$ by $0.109$: the
\emph{directional} readings are far less sensitive to the truncation than the norm ratio is, which
is why E4's conclusions rest on cosines.

\paragraph{Conclusions.}
At this width the model is over-parameterised relative to the batch, the requested operator change
is reachable up to the precision of the solve, and the ceiling is not what separates the arms.
Every difference reported below is a difference in what an optimiser \emph{does} with an available
direction, not in what the architecture allows. This is a negative result about a confound, and it
is what licenses the rest of Part II. The one caveat is procedural rather than structural: at
Adam's weights the solve is slow enough that $\alpha$ and the $\mathrm{ne}$ gate must be read as
budget-limited, and E4 marks the affected entries.

% ---------------------------------------------------------------------------
\subsubsection{E4 --- What each optimiser does with the reachable part}
\label{sec:e4}

\paragraph{Motivation and question.}
Given that the request is reachable, \emph{how much of it does each optimiser's step actually
deliver, and does an optimiser that misses in operator space also miss in weight space?} The two
can disagree, and the disagreement is informative: $\Delta W$ and $\Delta W^{\mathrm{ref}}$ may
differ by any element of $\ker M$ without changing the operator at all.

\paragraph{Setup.}
The common configuration. Each arm is probed along its own trajectory, and at every probe the three
reference objects --- the request $\Delta_\star$, its reachable part $\Pi\Delta_\star$, and the
minimum-norm update $M^{+}\Delta_\star$ realising it --- are recomputed from the weights that arm
currently holds. The comparison is therefore local and fair: each rule is asked how much of what
the loss wants \emph{at its own current position} its step delivers, and the step map $M$, the
target, and the ideal all belong to that position.

\paragraph{Measurement.}
Deflection in operator space and in weight space,
\begin{equation}
  \cos_{op} := \cos\big(M(\Delta W),\ \Pi\Delta_\star\big), \qquad
  \cos_{w}  := \cos\big(\Delta W,\ \Delta W^{\mathrm{ref}}\big),
  \label{eq:cosops}
\end{equation}
together with the decomposition of each step into $\mathrm{range}(M^\top)$ and $\ker M$. The
alignment probe runs every $2000$ steps, an order of magnitude coarser than the cheap probes, so
the convergence-phase reading here is the \emph{first alignment probe at or after} each run's
anchor rather than a mean over a window --- averaging a single measurement over eight probes would
report it as eight, and a window that happened to fall between two alignment probes would drop the
seed silently.

\paragraph{Status.} \textsc{done}.

\paragraph{Results.}

\begin{center}\small
\begin{tabular}{llccccc}
\toprule
architecture & arm & $\cos_{op}$ & $\cos_{w}$ & $\alpha$ & $\mathrm{ne}$ & $\ker M$ fraction \\
\midrule
ReLU   & gradient descent & $0.228 \pm 0.021$ & $0.122$  & $1.000$ & $1.5\cdot10^{-4}$ & $0.0000$ \\
       & Adam             & $0.032 \pm 0.016$ & $-0.002$ & $0.770^\ast$ & $8.9\cdot10^{-2\ast}$ & $\mathbf{0.813 \pm 0.081}$ \\
       & reference        & $0.941 \pm 0.007$ & $0.571$  & $1.000$ & $1.8\cdot10^{-5}$ & $0.0000$ \\
\midrule
CReLU  & gradient descent & $0.253 \pm 0.021$ & $0.272$  & $1.000$ & $9.1\cdot10^{-11}$ & $0.0000$ \\
       & Adam             & $0.034 \pm 0.012$ & $0.066$  & $1.000$ & $3.4\cdot10^{-2\ast}$ & $\mathbf{0.561 \pm 0.051}$ \\
       & reference        & $0.413 \pm 0.089$ & $0.213$  & $0.993$ & $1.9\cdot10^{-5}$ & $0.0000$ \\
\midrule
leaky  & gradient descent & $0.204 \pm 0.009$ & $0.132$  & $1.000$ & $9.5\cdot10^{-5}$ & $0.0000$ \\
       & Adam             & $0.066 \pm 0.012$ & $0.012$  & $0.993$ & $1.8\cdot10^{-2\ast}$ & --- \\
       & reference        & $0.860 \pm 0.016$ & $0.473$  & $1.000$ & $1.6\cdot10^{-4}$ & $0.0000$ \\
\bottomrule
\end{tabular}
\end{center}

\noindent$^\ast$ exceeds the $\mathrm{ne} > 10^{-2}$ quality gate of Equation~\eqref{eq:ne}; see
``the solve fails on Adam's weights'' below. Five of five ReLU seeds, four of five CReLU seeds and
three of three leaky seeds are affected, so the Adam row cannot simply be restricted to the passing
seeds.

\begin{figure}[H]
\centering
\includegraphics[width=0.49\textwidth]{alignment_relu.pdf}
\hfill
\includegraphics[width=0.49\textwidth]{alignment_crelu.pdf}
\caption{Deflection along training, ReLU (left) and CReLU (right). Each arm is scored along its own
trajectory, for the reason given in the setup. Adam's operator-space deflection sits near zero
throughout and is negative in several cells.}
\label{fig:alignment}
\end{figure}

\paragraph{Conclusions.}
Three facts, in decreasing order of confidence.

First, the decomposition is exact and structural: gradient descent and the reference lie
\emph{entirely} in $\mathrm{range}(M^\top)$, which is forced by
$\Delta W^{\mathrm{GD}} = M^\top\Delta_\star$ and $\Delta W^{\mathrm{ref}} = M^{+}\Delta_\star$.
The measurement confirms it and calibrates the method at the same time: both read $0.0000$, which
is the right calibration for a projection whose error one cannot otherwise see. Adam has a large
component that moves weights without moving the operator --- $0.56$ to $0.81$ of the step by norm
--- so most of what Adam does to the weights is invisible to the operator at the point where it is
done.

This is \emph{not} a property of adaptive methods, and an earlier version of this work said it was.
Section~\ref{sec:e18} measures the same quantity for five further optimisers and finds heavy ball,
which has no adaptivity whatsoever, at $0.478$ and $0.538$ --- beside Adam --- while K-FAC, a full
second-order preconditioner, sits at $0.013$ beside gradient descent. What the fraction tracks is
whether a rule accumulates a \emph{direction} across steps, and Section~\ref{sec:e15} exhibits the
mechanism by sweeping the momentum coefficient of the reference rule itself from $\beta = 0$, where
the fraction is exactly zero, to $\beta = 0.9$, where it is $0.596$. The Adam row above should
therefore be read as a statement about a rule with a first moment, not about adaptive
preconditioning.

The number is also sensitive to how it is measured, in a way worth recording. A step scored from an
empty state is not that rule's step: heavy ball with a zero buffer is plain gradient descent, and
Adam with $m = v = 0$ is $\mathrm{sign}(g)$. Measured that way Adam reads $0.503$ on ReLU against
$0.813$ with its own warmed state, and heavy ball reads $0.0002$ against $0.478$. Every figure here
uses each rule's own state, warmed for $300$ steps from the convergence-phase snapshot. The leaky
ReLU entry is left blank because that grid was not re-measured under the corrected procedure.

The projection also has to be run to convergence. At $400$ iterations the same measurement reports
$0.017$ for gradient descent and $0.112$ for the reference; only by $3000$ do the two structural
zeros actually appear. Every value in this paper uses $3000$ iterations.

Second, Adam's operator-space deflection is near zero and sometimes negative: its step is close to
orthogonal to what the loss asked the operator to do \emph{at the weights Adam itself holds}. This
is not a statement about its optimisation quality --- it reaches the lowest training loss of the
three --- but about the relationship between its weight step and the induced operator step. One
qualification attaches to the row: at Adam's weights the solve is budget-limited, so the target it
is scored against is only partially resolved.

\paragraph{The solve is slower at Adam's weights, and what that does and does not cost.}
Every Adam entry in the table exceeds the $\mathrm{ne} > 10^{-2}$ gate that Part II uses to discard
probes, in five of five ReLU seeds. We report the row rather than deleting it, with the gate
marked, because deleting it would silently remove the arm the experiment is about --- but the
reading has to be qualified. $\Pi\Delta_\star$ at an unconverged budget is a partial projection: it
still lies in $\mathrm{range}(M)$, so a cosine against it is a cosine against a genuine direction,
merely not the full one. Section~\ref{sec:e3} measures how much that matters by raising the budget
at an Adam checkpoint: over a range in which $\alpha$ moves by $0.109$, the cosine moves by
$0.017$. The norm ratio is the sensitive quantity and the direction is not, which is why $\alpha$
is footnoted in the table and the $\cos_{op}$ column is not. A reader who wants a conservative
statement can take this row as ``Adam's step is nearly orthogonal to the resolvable part of the
request'', which is what is measured and is enough for every use made of it below.

Third, and to be read with care: the reference arm's own $\cos_{op}$ is not $1$. The exact
reference satisfies $M(M^{+}\Delta_\star) = \Pi\Delta_\star$ identically, so a value below $1$
reflects the truncation at $k=50$ rather than any property of the rule. The number is therefore a
statement about how much of the reference fifty Krylov iterations capture at this depth, and it is
reported alongside gradient descent's --- which \emph{is} exactly the $k=1$ truncation
(Remark~\ref{rem:krylov}) --- with that distinction in mind. Section~\ref{sec:e17}
measures it as a function of $k$ and depth directly.

\paragraph{A note on the window.} Read at the convergence phase, the reference's $\cos_{op}$ is
$0.941$ on ReLU against $0.874$ averaged over the second half of training and $0.828$ over the last
quarter; on CReLU the three readings are $0.413$, $0.293$ and $0.283$, and on leaky $0.860$,
$0.815$ and $0.753$. The value falls monotonically the later the window is placed. The
same normalisation that drives the rank collapse of Section~\ref{sec:e9} is at work: past
interpolation the reference keeps stepping at constant size against a residual dominated by a few
hard samples, and its steps become less representative of what the loss is asking. The gap between
the two windows is thus not noise but the same mechanism measured twice, and it runs in the
direction that makes the endpoint understate the rule.

% ---------------------------------------------------------------------------
\subsubsection{E5 --- Endpoints: where each optimiser arrives}
\label{sec:e5}

\paragraph{Motivation and question.}
The diagnostics so far describe steps. \emph{Does following the operator gradient faithfully lead
to a better solution?} The framework's implicit promise is that it should; this experiment is the
test, and it is the central comparison of the paper.

\paragraph{Setup.}
The common configuration, five seeds (three for leaky ReLU), each arm at its best hyper-parameter by
the convergence-phase estimator. Crucially, every arm is run past interpolation, so the comparison
is at matched fit rather than at matched budget.

\paragraph{Measurement.}
Training loss and training accuracy over the convergence phase (to establish that the comparison is
at matched fit), and test accuracy over the same window.

\paragraph{Status.} \textsc{done}.

\paragraph{Results.}
``conv.\ step'' is where each arm first reaches training accuracy $0.999$, which is the anchor of
its own reading; the endpoint column is the last-quarter mean at the same hyper-parameter, shown so
that the two windows can be compared directly.

\begin{center}\small
\begin{tabular}{llccccc}
\toprule
architecture & arm & hyper & conv.\ step & train loss & train acc. & test acc. \\
\midrule
ReLU   & gradient descent & $0.1$    & $1050$ & $7.6\cdot10^{-4}$ & $1.000$ & $0.531 \pm 0.010$ \\
       & Adam             & $0.003$  & $7250$ & $9.5\cdot10^{-6}$ & $0.998$ & $\mathbf{0.634 \pm 0.013}$ \\
       & reference        & $0.5$    & $800$  & $7.7\cdot10^{-5}$ & $1.000$ & $0.556 \pm 0.005$ \\
\midrule
CReLU  & gradient descent & $0.03$   & $1000$ & $6.1\cdot10^{-3}$ & $1.000$ & $0.504 \pm 0.016$ \\
       & Adam             & $0.003$  & $3750$ & $5.9\cdot10^{-4}$ & $0.992$ & $\mathbf{0.612 \pm 0.071}$ \\
       & reference        & $10$     & $3550$ & $9.0\cdot10^{-4}$ & $1.000$ & $0.592 \pm 0.006$ \\
\midrule
leaky  & gradient descent & $0.1$    & $1000$ & $8.8\cdot10^{-4}$ & $1.000$ & $0.535 \pm 0.011$ \\
       & Adam             & $0.003$  & $5500$ & $1.7\cdot10^{-5}$ & $1.000$ & $\mathbf{0.654 \pm 0.018}$ \\
       & reference        & $1$      & $1167$ & $1.5\cdot10^{-3}$ & $1.000$ & $0.567 \pm 0.010$ \\
\bottomrule
\end{tabular}
\end{center}

\noindent Two remarks on the window. First, the selected hyper-parameter is identical in all nine
cells to the one the endpoint estimator selects, so the change of window moves the scores, not the
choice of run. Second, the scores move by at most $0.021$ and never in a way that changes the
ordering: gradient descent and Adam barely differ between windows because they have come to rest,
while the reference gains $0.021$ on CReLU and loses $0.016$ on leaky ReLU. The arms do not arrive
together --- the reference interpolates at step $800$ on ReLU against Adam's $7250$ --- and the
endpoint reading gives the reference nine times as long a post-interpolation tail as it gives Adam.
Adam's window mean training accuracy is $0.998$ on ReLU and $0.992$ on CReLU rather than exactly
$1.000$: the anchor probe is at $\ge 0.999$ by construction, but Adam oscillates across the
threshold for a few probes afterwards, which is visible here and is the same instability that
Figure~\ref{fig:selection} quantifies.

\begin{figure}[H]
\centering
\includegraphics[width=0.49\textwidth]{fit_vs_generalise.pdf}
\hfill
\includegraphics[width=0.49\textwidth]{rank_vs_accuracy.pdf}
\caption{Both panels read both axes in the same window, the convergence phase; hollow markers are
runs that never fit. Left: test accuracy against training loss --- the interpolating runs span four
orders of magnitude in loss without an accuracy trend, so the ordering is not a fitting effect.
Right: test accuracy against the operator rank each arm holds while it is learning. Reading rank at
the end of the budget instead moves the operator runs to the far left of this panel, which is the
post-interpolation collapse of Section~\ref{sec:e9} and not a property of the rule.}
\label{fig:endpoints}
\end{figure}

\paragraph{The comparison is at matched fit, and at matched loss.}
Every arm reaches training accuracy $\ge 0.999$ at its anchor. That alone is weaker than it sounds:
even read at matched phase the arms sit at training \emph{losses} one to two orders of magnitude
apart ($6.1\cdot10^{-3}$ for gradient descent on CReLU against $9.0\cdot10^{-4}$ for the reference,
and $7.6\cdot10^{-4}$ against $9.5\cdot10^{-6}$ on ReLU), and at logit margins that differ by more
than an order of magnitude (Section~\ref{sec:e16}). They are therefore not at comparable points
past the interpolation threshold, and one could object that the ordering below is a statement about
how far each arm has travelled rather than about where it went. We therefore also read each arm's
test accuracy off its trajectory at \emph{matched training loss}. This is an independent
phase-matching --- it anchors on a level of the loss rather than on the moment of interpolation ---
so its agreement with the table above is a check on both:

\begin{center}\small
\begin{tabular}{lcccc}
\toprule
ReLU, at training loss & $10^{-1}$ & $10^{-2}$ & $10^{-3}$ & $10^{-4}$ \\
\midrule
gradient descent & $0.518$ & $0.529$ & $0.531$ & $0.532$ \\
Adam             & $0.566$ & $0.628$ & $0.637$ & $\mathbf{0.641}$ \\
reference        & $0.515$ & $0.541$ & $0.551$ & $0.557$ \\
\midrule
CReLU, at training loss & $10^{-1}$ & $10^{-2}$ & $10^{-3}$ & $10^{-4}$ \\
\midrule
gradient descent & $0.495$ & $0.503$ & $0.508$ & --- \\
Adam             & $0.641$ & $0.620$ & $0.609$ & $\mathbf{0.613}$ \\
reference        & $0.547$ & $0.580$ & $0.592$ & $0.595$ \\
\bottomrule
\end{tabular}
\end{center}

\noindent The ordering gd $<$ reference $<$ Adam holds at every level and in all three
architectures, with one tie at the loosest ReLU level. It is therefore not an artefact of the arms
stopping at different depths of interpolation. The dash records that gradient descent never reaches
$10^{-4}$ on CReLU at any learning rate in its grid, which is itself part of the picture. The
matched-loss values also land on the convergence-phase table rather than on the endpoint one
($0.551$--$0.557$ against $0.556$ for the ReLU reference, $0.592$--$0.595$ against $0.592$ on
CReLU, where the endpoint reads $0.553$ and $0.571$) --- two different ways of matching phase,
agreeing with each other and disagreeing with the endpoint in the same direction.

\paragraph{Conclusions.}
The ordering is
\[
  \text{gradient descent } (0.50\text{--}0.54) \;<\;
  \text{reference } (0.56\text{--}0.59) \;<\;
  \text{Adam } (0.61\text{--}0.65),
\]
and it is the same in all three gated architectures.

The reference improves on gradient descent by $3$--$9$ points, consistently and in every
architecture. That is the positive part: making the weight step realise the requested operator step
does help, relative to the rule that differs from it only by Krylov truncation.

It does not close the gap to Adam, and this is the central negative result of the paper. The most
operator-faithful rule is not the best-generalising one; the \emph{least} faithful rule is. Sections
\ref{sec:e13}--\ref{sec:e16} pursue candidate explanations for that inversion --- when the advantage
is created, whether more training closes it, whether momentum is the hidden mechanism, and whether
robustness separates the arms --- and eliminate each.

% ---------------------------------------------------------------------------
\subsubsection{E6 --- Calibrating the scale: what do these accuracies mean?}
\label{sec:e6}

\paragraph{Motivation and question.}
A $2$--$6$ point improvement is only interesting against a scale. \emph{Where do these accuracies
sit relative to methods that use no learned representation at all?}

\paragraph{Setup.} $k$-nearest neighbours on the same split, Euclidean distance on raw inputs,
$k \in \{1,3,5,10,20,50\}$; multinomial logistic regression on raw inputs; and for context the
published MNIST-1D baselines for an MLP and a CNN.

\paragraph{Status.} \textsc{done}.

\paragraph{Results.}
$k$-NN reaches $0.552$ at $k=1$, $0.563$ at $k=5$, $0.580$ at $k=10$ and $0.458$ at $k=50$.
Logistic regression on raw inputs reaches $0.32$. For reference, a tuned MLP reaches $\approx 0.68$
and a CNN $\approx 0.94$ on this task. Placing our arms on the same line:
\begin{align*}
 \text{logistic } 0.32 \;&<\;
 \text{gd } 0.50\text{--}0.54 \;<\;
 \text{reference } 0.56\text{--}0.59 \;\approx\; \text{$k$-NN } 0.58 \\
 &<\; \text{Adam } 0.61\text{--}0.65 \;<\; \text{MLP } 0.68 \;<\; \text{CNN } 0.94 .
\end{align*}

\paragraph{Conclusions.}
The reference generalises at roughly $k$-nearest-neighbour level. It clears logistic regression by
a wide margin, so it is learning a genuinely nonlinear function, but it does not exceed what a
memorising predictor achieves on this task. Adam does. This calibration is worth stating plainly
because it bounds how much the reference's improvement over gradient descent can be claimed to
mean: the improvement is real and consistent, and it lands at the level of a method with no
representation learning at all.

% ---------------------------------------------------------------------------
\subsubsection{E7 --- The bias dial: interpolating from gradient descent to the reference}
\label{sec:e7}

\paragraph{Motivation and question.}
Gradient descent and the reference are the two ends of one family: $\Delta W = $ the $k$-step
Krylov truncation of $M^{+}\Delta_\star$, with $k=1$ recovering gradient descent exactly
(Remark~\ref{rem:krylov}). \emph{Is generalisation monotone along this family, and is
there an optimal amount of operator faithfulness?} This is the cleanest available test because $k$
varies the bias with the architecture, data, loss and seed all held fixed.

\paragraph{Setup.}
ReLU and CReLU, the common configuration, $\eta \in \{0.1, 0.3, 1, 3, 10\}$ crossed with
$k \in \{1, 2, 5, 20, 50, 200\}$, three seeds (five at $k=50$).

\paragraph{Measurement.}
Test accuracy, training loss and accuracy, $\cos_{op}$ and operator rank at the convergence phase,
per cell.

\paragraph{Status.} \textsc{done}.

\paragraph{Results.} At $\eta = 1$:

\begin{center}\small
\begin{tabular}{lcccccc}
\toprule
$k$ (ReLU) & $1$ & $2$ & $5$ & $20$ & $50$ & $200$ \\
\midrule
test accuracy   & ---     & $0.465$ & $0.486$ & $0.564$ & $\mathbf{0.565}$ & $0.528$ \\
train accuracy  & $0.879$ & $0.993$ & $1.000$ & $1.000$ & $1.000$ & $1.000$ \\
$\cos_{op}$     & ---     & $0.135$ & $0.365$ & $0.800$ & $0.861$ & $0.948$ \\
operator rank   & ---     & $5.69$  & $7.20$  & $7.27$  & $6.55$  & $7.13$ \\
\midrule
$k$ (CReLU) & $1$ & $2$ & $5$ & $20$ & $50$ & $200$ \\
\midrule
test accuracy   & $0.478$ & $0.487$ & $0.528$ & $\mathbf{0.602}$ & $0.592$ & $0.558$ \\
train accuracy  & $1.000$ & $1.000$ & $1.000$ & $1.000$ & $1.000$ & $1.000$ \\
$\cos_{op}$     & $0.140$ & $0.237$ & $0.562$ & $0.864$ & $0.931$ & $0.992$ \\
operator rank   & $5.99$  & $6.12$  & $7.47$  & $7.22$  & $7.16$  & $7.71$ \\
\bottomrule
\end{tabular}
\end{center}

\noindent The ReLU $k=1$ column is empty because that cell never reaches training accuracy
$0.999$: it has no convergence phase, and the dash is the honest entry rather than a number read
from a run that never fit. Its training accuracy is reported so the failure is visible.

\begin{figure}[H]
\centering
\includegraphics[width=0.49\textwidth]{eta_k_heatmap.pdf}
\hfill
\includegraphics[width=0.49\textwidth]{eta_k_dynamics_relu.pdf}
\caption{Left: test accuracy over the $(\eta, k)$ grid. Right: training dynamics along the dial for
ReLU, showing that the low-$k$ cells are slower as well as worse.}
\label{fig:dial}
\end{figure}

\paragraph{Conclusions.}
$\cos_{op}$ increases monotonically with $k$, as it must. Test accuracy increases with $k$ up to
$k \approx 20$--$50$ and then \emph{falls} at $k = 200$, in both architectures.

We deliberately do not claim an interior optimum. On ReLU the $k \in \{1,2\}$ cells do not reach
training accuracy $1.000$, so part of the rise from $k=2$ to $k=5$ is a fitting effect rather than a
generalisation effect; on CReLU the whole dial interpolates and the rise survives, which is why both
rows are shown. The decline at $k=200$ is the more robust half of the shape and is present in both.
What the dial establishes safely is that generalisation improves as the step is made more
operator-faithful over most of the range, and that this relationship is not monotone to the end.

The operator-rank row is flat, and its flatness is a result. At the convergence phase every cell of
the dial sits between $5.7$ and $7.7$ --- the rank does not vary with $k$ at all, in either
architecture. Read at the end of the budget the same row runs from $1.50$ to $7.76$ and appears to
carry a strong $k$-dependence. That apparent dependence is the post-interpolation decay of
Section~\ref{sec:e9} timed differently in each cell: the cells that interpolate early (at $k=20$,
by step $1000$) have eleven thousand steps in which to decay and the cells that interpolate late
have one or two, so the endpoint row mostly measures \emph{when} each cell finished fitting. The
dial changes how faithfully the step is taken, not the complexity of the operator it arrives at.

% ---------------------------------------------------------------------------
\subsubsection{E8 --- How much of the nonlinearity each optimiser uses}
\label{sec:e8}

\paragraph{Motivation and question.}
A gated network's expressive power comes from gates that \emph{switch} across inputs; a unit always
on or always off contributes a fixed linear map. \emph{Do the arms differ in how much of the gating
they actually use, and does that track generalisation?}

\paragraph{Setup.} The common configuration, gate patterns evaluated on $256$ training inputs at
the convergence-phase snapshot. The count matters and is fixed across arms: a unit is called switching if its
state is not constant over the probe set, so the measured fraction can only rise as the probe
grows. On CReLU the reference reads $0.728$ at $n = 256$, $0.819$ at $n = 1024$ and $0.856$ at the
full $4000$, while gradient descent is pinned at $1.000$ throughout. The \emph{ordering} is
therefore robust to $n$ and the \emph{magnitude} is not, so the percentages below should be read
as a comparison at fixed $n$ rather than as a property of the network alone.

\paragraph{Measurement.} The fraction of units that switch state across inputs, and the fractions
always on and always off. For CReLU the gate is the sign pattern of the pre-activation.

\paragraph{Status.} \textsc{done}.

\paragraph{Results.}

\begin{center}\small
\begin{tabular}{llcccc}
\toprule
architecture & arm & switching & always on & always off & test acc. \\
\midrule
ReLU  & gradient descent & $0.996$ & $0.000$ & $0.004$ & $0.531$ \\
      & Adam             & $0.915$ & $0.000$ & $0.085$ & $0.634$ \\
      & reference        & $0.780$ & $0.133$ & $0.087$ & $0.556$ \\
\midrule
leaky & gradient descent & $0.999$ & $0.000$ & $0.001$ & $0.535$ \\
      & Adam             & $0.995$ & $0.000$ & $0.005$ & $0.654$ \\
      & reference        & $0.794$ & $0.107$ & $0.099$ & $0.567$ \\
\midrule
CReLU & gradient descent & $0.999$ & $0.000$ & $0.000$ & $0.504$ \\
      & Adam             & $0.997$ & $0.002$ & $0.001$ & $0.612$ \\
      & reference        & $0.757$ & $0.119$ & $0.123$ & $0.592$ \\
\bottomrule
\end{tabular}
\end{center}

\noindent This is the one structural measure on which the choice of window changes almost nothing.
The baselines are identical to three decimals in both windows, and the reference's switching
fraction moves by $0.04$--$0.05$ ($0.780$ against $0.737$ at the endpoint on ReLU, $0.757$ against
$0.706$ on CReLU) --- in the direction of slightly \emph{more} gating used at convergence, and far
too small to close the gap to the baselines.

\begin{figure}[H]
\centering
\includegraphics[width=0.49\textwidth]{gates_relu.pdf}
\hfill
\includegraphics[width=0.49\textwidth]{gates_crelu.pdf}
\caption{Gate statistics along training for ReLU (left) and CReLU (right).}
\label{fig:gates}
\end{figure}

\paragraph{Conclusions.}
The reference \emph{under-uses its gating}: it pins $21$--$24\%$ of units to a constant state, split
roughly evenly between always-on and always-off, while both baselines leave essentially every unit
switching. This is the clearest structural signature separating the reference from both baselines,
it is consistent across all three architectures, and --- unlike the operator-complexity measures of
Section~\ref{sec:e9} --- it is stable to the choice of window, which makes it the most robust of
them.

The leaky-ReLU column is the control that makes this interpretable. Leaky ReLU has no dead units by
construction --- every gate is strictly positive --- yet the reference still pins $107$ of every
$1000$ units to the always-on state and still generalises between the two baselines. So the effect
is not unit death, and pruning is not the mechanism: it is a reduction in \emph{input-dependence} of
the gate pattern.

The signature does not order the arms by accuracy. Adam and gradient descent both sit near $1.0$
switching and differ by $10$--$12$ points of test accuracy.

% ---------------------------------------------------------------------------
\subsubsection{E9 --- Operator rank and operator diversity}
\label{sec:e9}

\paragraph{Motivation and question.}
$P(x)$ is a linear map per input. Two different quantities can be called its complexity: the rank of
a single $P(x)$, and how much $P(x)$ \emph{varies} across inputs. \emph{Do the arms differ on
either, and are the two the same story?}

\paragraph{Setup.} The common configuration, evaluated on $512$ training inputs. Both measures need
the weights rather than a logged scalar, so the convergence phase is quantised to the snapshot grid:
each seed is read at its own earliest snapshot at or after the step where that run first reaches
training accuracy $0.999$. The final-checkpoint value is reported alongside, because in this
experiment the two windows do not merely differ in magnitude --- they reverse the ordering.

\paragraph{Measurement.}
Pointwise operator rank is the participation ratio
$\mathrm{PR}(P) = (\sum_i \sigma_i)^2 / \sum_i \sigma_i^2$ of the singular values of $P(x)$,
averaged over inputs with annihilated inputs excluded and reported separately. Operator diversity is
the participation ratio of the stacked, centred $\{\mathrm{vec}\,P(x_i)\}$, which measures how many
directions the operator explores across the dataset.

\paragraph{Status.} \textsc{done}.

\paragraph{Results.}
$n = 512$ inputs, five seeds (three for leaky ReLU). The left block is the convergence phase, the
right block the final checkpoint.

\begin{center}\small
\begin{tabular}{llcccc|ccc}
\toprule
 & & \multicolumn{4}{c|}{at the convergence phase} & \multicolumn{3}{c}{at the final checkpoint} \\
architecture & arm & rank & diversity & \% init. & shared & rank & diversity & shared \\
\midrule
ReLU  & initialisation   & ---             & $241.2$                   & $100\%$         & ---
                         & ---             & ---                       & --- \\
      & gradient descent & $5.72$          & $207.3 \pm 2.3$           & $86\%$          & $0.068$
                         & $5.75$          & $206.3$                   & $0.069$ \\
      & Adam             & $1.98$          & $161.7 \pm 11.3$          & $67\%$          & $0.014$
                         & $1.98$          & $161.4$                   & $0.012$ \\
      & reference        & $\mathbf{6.99}$ & $\mathbf{231.7 \pm 5.0}$  & $\mathbf{96\%}$ & $0.017$
                         & $1.58$          & $37.9$                    & $0.008$ \\
\midrule
CReLU & initialisation   & ---             & $266.3$                   & $100\%$         & ---
                         & ---             & ---                       & --- \\
      & gradient descent & $\mathbf{6.58}$ & $\mathbf{212.9 \pm 4.4}$  & $\mathbf{80\%}$ & $0.117$
                         & $6.53$          & $210.5$                   & $0.114$ \\
      & Adam             & $2.91$          & $96.7 \pm 19.1$           & $36\%$          & $0.010$
                         & $3.03$          & $104.9$                   & $0.010$ \\
      & reference        & $6.05$          & $172.5 \pm 46.7$          & $65\%$          & $0.043$
                         & $3.32$          & $83.9$                    & $0.100$ \\
\midrule
leaky & initialisation   & ---             & $232.9$                   & $100\%$         & ---
                         & ---             & ---                       & --- \\
      & gradient descent & $6.01$          & $197.2 \pm 4.9$           & $85\%$          & $0.091$
                         & $6.03$          & $195.8$                   & $0.093$ \\
      & Adam             & $3.89$          & $192.3 \pm 4.9$           & $83\%$          & $0.027$
                         & $3.91$          & $193.3$                   & $0.027$ \\
      & reference        & $\mathbf{6.85}$ & $\mathbf{203.6 \pm 18.3}$ & $\mathbf{87\%}$ & $0.027$
                         & $2.70$          & $78.3$                    & $0.013$ \\
\bottomrule
\end{tabular}
\end{center}

\noindent Diversity is a participation ratio over $n$ stacked operators, so its ceiling is $n$ and
it rises with the probe size for every arm. The column is a comparison at fixed $n = 512$, not a
quantity intrinsic to the network --- the same caution as for the switching fraction in E8.

\paragraph{Two distinct findings, one per phase.}
\emph{While it learns, the reference keeps the most operator complexity, which is what the
construction predicts.} The low-rank bias of a factored model is usually attributed to the
parameterisation: depth makes the gradient step favour directions that are already large, and that
is what drives the solution toward low rank \citep{gunasekar2017implicit,arora2019implicit}. The
reference step removes the parameterisation's contribution by construction, so it should
\emph{not} inherit that bias, and at the convergence phase it does not: it holds the highest
pointwise rank in two of three architectures and the highest diversity in two of three, above even
its own initialisation in rank. Adam, the least operator-faithful rule, is lowest on both in all
three. This is the prediction coming out right, and it is worth stating separately from what
follows.

\emph{After it has fitted, the reference --- alone among the three --- keeps moving, and the
complexity collapses.} Both baselines come to rest: gradient descent goes $207.3 \to 206.3$ in
diversity on ReLU between the two windows, Adam $161.7 \to 161.4$, and on leaky $197.2 \to 195.8$
and $192.3 \to 193.3$. The reference goes $231.7 \to 37.9$, a factor of six. That asymmetry is the
signature of Remark~\ref{rem:normalise}: the baselines' steps shrink with the residual and stop,
while the reference's step size is fixed at $\eta$ and it continues at constant speed against a
normalised residual dominated by whichever few samples are least well fit. Both phases are
reported throughout, because the second is a real property of the rule as implemented here and not
a measurement artefact --- it is just a property of what it does \emph{after} the task is solved.

\begin{figure}[H]
\centering
\includegraphics[width=0.49\textwidth]{operator_diversity.pdf}
\hfill
\includegraphics[width=0.49\textwidth]{pointwise_vs_diversity.pdf}
\caption{Operator diversity along training (left), with a star and dashed line marking where each
arm first fits the training set --- everything to the right of a marker is that arm's
post-interpolation tail, and the reference's is where its diversity falls away --- and the
relationship between pointwise rank and diversity across arms (right).}
\label{fig:diversity}
\end{figure}

\paragraph{The trajectory, which is what makes the two windows interpretable.}
Pointwise rank along training on ReLU, all arms from the same initialisation:

\begin{center}\small
\begin{tabular}{lcccccc}
\toprule
step & $0$ & $1000$ & $2000$ & $4000$ & $8000$ & $12000$ \\
\midrule
gradient descent & $7.02$ & $5.60$ & $5.69$ & $5.70$ & $5.71$ & $5.72$ \\
Adam             & $7.02$ & $3.86$ & $3.42$ & $2.79$ & $2.02$ & $1.98$ \\
reference        & $7.02$ & $\mathbf{7.67}$ & $\mathbf{7.30}$ & $\mathbf{7.01}$ & $3.04$ & $1.58$ \\
\midrule
reference, train loss & $3.1$ & $3.5\cdot10^{-3}$ & $9.3\cdot10^{-6}$ & $2.4\cdot10^{-8}$
 & $2.9\cdot10^{-9}$ & $1.4\cdot10^{-10}$ \\
\bottomrule
\end{tabular}
\end{center}

\noindent Operator diversity over the same trajectory, from the snapshot grid, showing that the
two measures move together and that the collapse is confined to one arm:

\begin{center}\small
\begin{tabular}{lccccc}
\toprule
ReLU, diversity at step & $0$ & $4000$ & $8000$ & $12000$ & fits at step \\
\midrule
gradient descent & $239.8$ & $207.3$ & $206.5$ & $206.4$ & $1050$ \\
Adam             & $241.6$ & $190.6$ & $161.7$ & $161.4$ & $7250$ \\
reference        & $239.8$ & $\mathbf{230.0}$ & $83.9$ & $37.3$ & $800$ \\
\bottomrule
\end{tabular}
\end{center}

\noindent Gradient descent is at its final value by step $4000$ and moves by $0.9$ thereafter.
Adam declines while it is still fitting --- it does not reach training accuracy $0.999$ until step
$7250$ --- and is flat afterwards. The reference is the only arm that is still at $96\%$ of its
initial diversity at step $4000$, more than three thousand steps after it has fitted, and then
loses three quarters of it over the remaining eight thousand.

\noindent While the reference is learning it holds the \emph{highest} operator rank of the three,
above even its own initialisation, and it does so through step $4000$ --- by which point the
training loss is $2.4\cdot10^{-8}$ and the task is long since fit. Adam collapses the rank
monotonically from the first probe, and gradient descent settles within a thousand steps and stops.
The reference's collapse happens entirely \emph{after} interpolation.

That this is post-interpolation behaviour rather than a property of the rule can be checked
directly, in two ways. Across all $54$ operator-arm runs, the collapse begins after the run
interpolates in $51$, with a median lag of $2500$ steps; the single run that never interpolates
(CReLU at $\eta = 10$) is also the one whose rank never collapses, ending at $7.35$. And removing
the normalisation removes the collapse: an arm stepping with the raw target $\Delta_\star = -\eta G$
instead of the normalised one holds rank $6.27$ at step $12000$ where the normalised arm reads
$1.59$, while its training loss plateaus at $6.4\cdot10^{-4}$ rather than being driven to
$8\cdot10^{-11}$.

\paragraph{Conclusions.}
Gradient descent barely reorganises the operator at any point: it settles at rank $5.7$--$6.6$
within the first thousand steps and stays there, keeping $80$--$86\%$ of its initial diversity in
both windows. Adam reduces both steadily while it trains and then also stops. The reference
preserves both while it learns --- at convergence it retains $96\%$, $65\%$ and $87\%$ of its
initial diversity against Adam's $67\%$, $36\%$ and $83\%$ --- and reduces them only afterwards.

The correct statement is therefore not that the reference has a low-rank bias. While it learns it
is the arm that preserves operator complexity best, which is what one would expect of a rule that
removes the parameterisation's contribution to the step; a low-rank bias is what shows up if one
measures it after the loss has nothing left to ask of it, and that is a statement about the
normalisation of Remark~\ref{rem:normalise} rather than about following the operator gradient.

None of these orderings matches the accuracy ordering, in either window, so the measure remains an
elimination rather than an explanation --- but the elimination is now sharper than it was. At the
endpoint one could at least tell a story in which low operator complexity accompanies poor
generalisation, since the reference was lowest on both. At the convergence phase that story is
unavailable: the reference is the most complex of the three while it learns and still generalises
between the two baselines, and on leaky ReLU Adam and gradient descent sit at $83\%$ and $85\%$
diversity while differing by $12$ points of accuracy. Operator complexity, pointwise or across
inputs, does not predict generalisation here in either direction.

The \emph{shared} column is worth a line, and it too reads differently at the two windows. At the
endpoint on CReLU the reference retains a large common component ($0.100$ against Adam's $0.010$),
suggesting its collapse is towards a single shared map; at the convergence phase that component is
$0.043$, less than gradient descent's $0.117$. The common component is therefore something the
reference acquires during the collapse, not something it builds while learning --- which is
consistent with the collapse being a drift towards a single direction rather than a form of
structure.

\subsubsection{E10 --- What the representation looks like layer by layer}
\label{sec:e10}

\paragraph{Motivation and question.}
E9 measures the operator, which is what the framework is about, but it says nothing directly about
the \emph{representation} the network builds on the way. \emph{Do the arms organise class structure
across depth differently, and is the operator-rank difference of E9 visible in the hidden
activations?}

\paragraph{Setup.} The common configuration, the convergence-phase snapshot, $400$ test inputs,
five seeds. For each arm we take the hidden activation at layers $1, 2, 4, 6, 8$ and project onto
its first two principal components, colouring by class.

\paragraph{Measurement.} The fraction of activation variance captured by those two components,
which is a direct measure of how low-dimensional the representation has become, and the visible
class organisation.

\paragraph{Status.} \textsc{done}.

\paragraph{Results.}
Fraction of variance in the first two principal components:

\begin{center}\small
\begin{tabular}{llccccc|cc}
\toprule
 & & \multicolumn{5}{c|}{at the convergence phase} & \multicolumn{2}{c}{endpoint} \\
architecture & arm & layer 1 & layer 2 & layer 4 & layer 6 & layer 8 & layer 6 & layer 8 \\
\midrule
ReLU  & gradient descent & $23\%$ & $21\%$ & $26\%$ & $45\%$ & $80\%$ & $45\%$ & $80\%$ \\
      & Adam             & $21\%$ & $17\%$ & $41\%$ & $\mathbf{72\%}$ & $\mathbf{83\%}$ & $74\%$ & $83\%$ \\
      & reference        & $24\%$ & $24\%$ & $35\%$ & $58\%$ & $52\%$ & $86\%$ & $100\%$ \\
\midrule
CReLU & gradient descent & $34\%$ & $27\%$ & $29\%$ & $45\%$ & $86\%$ & $46\%$ & $85\%$ \\
      & Adam             & $21\%$ & $18\%$ & $42\%$ & $\mathbf{62\%}$ & $\mathbf{95\%}$ & $61\%$ & $94\%$ \\
      & reference        & $29\%$ & $26\%$ & $39\%$ & $63\%$ & $73\%$ & $82\%$ & $92\%$ \\
\bottomrule
\end{tabular}
\end{center}

\begin{figure}[H]
\centering
\includegraphics[width=\textwidth]{layer_reps_relu.pdf}
\caption{Two-dimensional PCA of the hidden representation at layers $1$, $4$ and $8$ for each arm
on ReLU, coloured by class, at each arm's convergence-phase snapshot (the step is given in the
first panel of each row, and the arms reach it at very different times). Adam separates classes
along a low-dimensional filament by layer $4$; gradient descent stays diffuse until the last layer;
the reference is intermediate and does not collapse --- the near-total collapse visible in this
figure when it is drawn at the final checkpoint happens after interpolation, not while the rule is
learning.}
\label{fig:layerreps}
\end{figure}

\paragraph{Conclusions.}
The three arms compress the representation at different depths and to different degrees. Gradient
descent stays diffuse --- two components explain only $21$--$29\%$ of the variance through layer $4$
--- and compresses only at the output. Adam compresses earliest and hardest, reaching $41$--$42\%$
by layer $4$ and $83$--$95\%$ at layer $8$, with the class structure visibly organised along a
one-dimensional filament from layer $4$ onward. The reference is intermediate through layer $6$ and
then, on ReLU, \emph{decompresses}: its layer-$8$ representation is the least two-dimensional of
the three at $52\%$.

This corroborates E9 in a different measurement, including in the part of E9 that depends on the
window. Read at the final checkpoint the same table gives the reference $100\%$ at layer $8$ on
ReLU --- an exactly two-dimensional final representation --- and that reading was the basis of an
earlier claim that the reference compresses hardest. At the convergence phase it is $52\%$. The
baselines move by at most two points between the windows and the reference moves by $48$, which is
the same asymmetry, in the same direction, that Section~\ref{sec:e9} finds in operator rank and
diversity: what looked like the reference's characteristic representation was the endpoint of a
post-interpolation collapse, visible in the activations exactly as it is in $P(x)$.

What survives, and is unaffected by the window, is the statement about Adam: it is the arm that
builds a low-dimensional, class-organised representation \emph{early}, by layer $4$, and it is the
best-generalising arm. The reference does not do this at any depth, in either window.

\subsubsection{E11 --- Where the linear regions sit}
\label{sec:e11}

\paragraph{Motivation and question.}
A gated network partitions input space into linear regions. \emph{Do the arms place their regions
differently relative to the data, and does placement predict generalisation?} The natural hypothesis
is that a method which spends its partition near the data --- where decisions are made --- and keeps
the function simple away from it should generalise better.

\paragraph{Setup.}
Two-dimensional slices through three MNIST-1D training points of distinct classes, spanning the
affine plane they define, sampled on a $240\times240$ grid. Eight planes per seed with the triple
resampled each time, three seeds, for $24$ planes per architecture --- so that what survives is a
property of the function rather than of one viewpoint. Local complexity is measured both over all
gates and restricted to live units.

\paragraph{Measurement.}
Partition-edge density as a function of distance from the nearest training point, and the
\emph{concentration contrast}: mean density within $0.25$ plane units of a training point minus
mean density beyond $0.75$. Comparisons are paired across planes, so each plane contributes a
difference and the test is on those differences. Weights are read at each arm's convergence-phase
snapshot.

\paragraph{Status.} \textsc{done}.

\paragraph{Results.} $24$ planes per architecture ($8$ planes $\times$ $3$ seeds).

\begin{center}\small
\begin{tabular}{lccc}
\toprule
pair (ReLU) & contrast & $t$ & same sign \\
\midrule
reference $-$ gd   & $+0.0830 \pm 0.0279$ & $+14.3$ & $100\%$ of planes \\
reference $-$ Adam & $+0.0620 \pm 0.0479$ & $+6.2$  & $92\%$ of planes \\
Adam $-$ gd        & $+0.0209 \pm 0.0485$ & $+2.1$  & $71\%$ of planes \\
\midrule
pair (CReLU) & contrast & $t$ & same sign \\
\midrule
reference $-$ gd   & $+0.1104 \pm 0.1068$ & $+5.0$ & $71\%$ of planes \\
reference $-$ Adam & $+0.1279 \pm 0.1136$ & $+5.4$ & $79\%$ of planes \\
Adam $-$ gd        & $-0.0175 \pm 0.0320$ & $-2.6$ & $67\%$ of planes \\
\bottomrule
\end{tabular}
\end{center}

\noindent Read at the final checkpoint instead, the same contrasts are $+0.0948$, $+0.0719$,
$+0.0228$ and $+0.1547$, $+0.1538$, $+0.0009$: the reference's separation from both baselines is
somewhat larger there but present in both windows and in both architectures, which is why this row
is reported as robust. The Adam-minus-gradient-descent line is the one that is not robust --- it is
$+0.0209$ on ReLU and $-0.0175$ on CReLU at the convergence phase, small and of opposite sign in
the two architectures --- and that is the comparison the measure would have needed to settle to
explain the accuracy ordering.

\begin{figure}[H]
\centering
\includegraphics[width=0.49\textwidth]{local_complexity_relu.pdf}
\hfill
\includegraphics[width=0.49\textwidth]{complexity_avg_relu.pdf}
\caption{Left: local complexity along training. Right: partition-edge density against distance from
the nearest training point at each arm's convergence-phase snapshot, averaged over $24$ random
planes with a one-standard-deviation band.}
\label{fig:regions}
\end{figure}

\begin{figure}[H]
\centering
\includegraphics[width=0.49\textwidth]{slice_relu.pdf}
\hfill
\includegraphics[width=0.49\textwidth]{slice_knn1_relu.pdf}
\caption{A single data plane: the three arms' partitions and decision boundaries (left), and
$1$-nearest-neighbour on the same plane (right), included because $k$-NN is the accuracy-matched
comparator from Section~\ref{sec:e6}.}
\label{fig:slice}
\end{figure}

\paragraph{Conclusions.}
The reference concentrates its partition on the data more strongly than either baseline, and the
effect is large, consistent and paired-significant --- $t = +5.0$ to $+14.3$, present in
$71$--$100\%$ of planes depending on the pair and architecture. This is the second structural
signature separating the reference from both baselines, and like the switching fraction of
Section~\ref{sec:e8} it holds in both measurement windows.

It does not explain generalisation. Adam and gradient descent differ by $|t| \le 2.6$ on this
measure, with the sign of the difference reversing between the two architectures, while differing
by $10$--$11$ points of test accuracy in both. So concentration separates the reference from the
baselines but is silent on exactly the gap that matters.

We also record a refuted hypothesis. The natural reading --- that density should \emph{decrease}
away from the data for a method that generalises --- is false here: density decreases with distance
for all three arms, and the sign of the between-arm difference flips by architecture. Region
placement is not the mechanism.

% ---------------------------------------------------------------------------
\subsubsection{E12 --- Capacity: does a smaller reference-trained network close the gap?}
\label{sec:e12}

\paragraph{Motivation and question.}
One reading of E8 and E9 is that the reference uses less of the network than it is given. \emph{If
so, does a smaller network trained with the reference match Adam's accuracy at full width?} If the
reference's disadvantage were excess capacity used badly, shrinking the network should help it more
than it helps the baselines.

\paragraph{Setup.} CReLU, widths $\{16, 32, 64, 128\}$, all three arms, full hyper-parameter grids,
two to five seeds per cell.

\paragraph{Status.} \textsc{done}.

\paragraph{Results.}

\begin{center}\small
\begin{tabular}{lccc}
\toprule
width & gradient descent & Adam & reference \\
\midrule
$16$  & $0.376$ \,(\,$0.880$\,) & $\mathbf{0.477}$ \,(\,$0.906$\,) & $0.420$ \,(\,$1.000$\,) \\
$32$  & $\mathbf{0.515}$ \,(\,$1.000$\,) & $0.478$ \,(\,$1.000$\,) & $0.495$ \,(\,$1.000$\,) \\
$64$  & $0.475$ \,(\,$1.000$\,) & $\mathbf{0.636}$ \,(\,$0.999$\,) & $0.561$ \,(\,$1.000$\,) \\
$128$ & $0.511$ \,(\,$1.000$\,) & $\mathbf{0.613}$ \,(\,$1.000$\,) & $0.571$ \,(\,$1.000$\,) \\
\bottomrule
\end{tabular}
\end{center}

\noindent Each entry is the best cell of that arm's full grid, with the highest training accuracy
that cell reaches in parentheses. The comparison is deliberately \emph{not} restricted to cells
that fit: at width $16$ the question is how well each optimiser does with the capacity it is given,
and the fact that two of the three cannot fit $4000$ points with a width-$16$ CReLU network at any
learning rate in their grid is part of the answer rather than a reason to exclude them. Read at the
convergence phase instead --- that is, restricted to fitting cells --- the three widths that fit
throughout give $0.519/0.484/0.495$, $0.472/0.640/0.566$ and $0.504/0.612/0.592$, with the same
hyper-parameter selected in every cell, and width $16$ has an entry only for the reference
($0.429$), the one arm that converges there.

\begin{figure}[H]
\centering
\includegraphics[width=0.7\textwidth]{capacity.pdf}
\caption{Test accuracy against width for the three arms: each arm's best cell over its whole grid,
scored at the endpoint. Hollow markers are cells that never reach training accuracy $0.999$ --- at
width $16$ only the reference fits --- and crosses overlay the convergence-phase value wherever the
arm has one.}
\label{fig:capacity}
\end{figure}

\paragraph{Conclusions.}
The hypothesis is refuted. The reference's accuracy \emph{increases} with width throughout, from
$0.420$ at width $16$ to $0.571$ at width $128$, tracking the baselines rather than peaking at a
smaller model. Excess capacity is not what the reference is failing to exploit: taking capacity
away hurts it at least as much as it hurts the others, and it never overtakes Adam at any width.

The width-$16$ row is worth reading for a second reason, unrelated to the hypothesis. It is the
only setting in this paper where the reference is the \emph{only} arm that fits: it reaches
training accuracy $1.000$ while gradient descent tops out at $0.880$ and Adam at $0.906$ across
their whole grids. Adam still generalises best there ($0.477$ against $0.420$), so fitting is not
what is buying the accuracy --- but at the tightest capacity the operator-faithful step is the one
that can still drive the training loss down, which is consistent with the deep-linear picture of
Part~I and with the reference's advantage over gradient descent being largest where the step map is
hardest to invert.

Restricted to fitting cells the picture at width $16$ changes: Adam's only converging cell scores
$0.382$, below the reference's $0.429$. We report the unrestricted comparison as the headline
because the question is about capacity rather than about matched fit, and flag the restricted one
here because it is the comparison the rest of Part II would make.

% ---------------------------------------------------------------------------
\subsubsection{E13 --- When is the advantage created? The optimiser-switch experiment}
\label{sec:e13}

\paragraph{Motivation and question.}
The endpoint comparison of E5 says \emph{that} Adam ends better; it says nothing about \emph{when}
the advantage is established. \emph{Is Adam's benefit created early and merely preserved, or does it
require Adam throughout?} If it is created early, the search for a mechanism narrows to the first
few thousand steps.

\paragraph{Setup.}
A run is trained with one rule for the first $s$ steps and the other for the remaining $12000 - s$,
with $s \in \{0, 250, 1000, 3000, 6000, 12000\}$, in both directions, on ReLU and CReLU, three
seeds. The endpoints are internal checks: $s=0$ reproduces the pure second arm and $s=12000$ the
pure first, and both do.

\paragraph{Status.} \textsc{done}.

\paragraph{Results.}

\begin{center}\small
\begin{tabular}{lcccccc}
\toprule
handover step $s$ & $0$ & $250$ & $1000$ & $3000$ & $6000$ & $12000$ \\
\midrule
ReLU, Adam then reference  & $0.548$ & $0.554$ & $0.574$ & $0.601$ & $0.606$ & $0.645$ \\
ReLU, reference then Adam  & $0.645$ & $0.636$ & $0.613$ & $0.562$ & $0.547$ & $0.548$ \\
\midrule
CReLU, Adam then reference & $0.561$ & $0.581$ & $0.645$ & $\mathbf{0.660}$ & $0.609$ & $0.596$ \\
CReLU, reference then Adam & $0.596$ & $0.640$ & $0.608$ & $0.527$ & $0.503$ & $0.561$ \\
\bottomrule
\end{tabular}
\end{center}

\noindent Standard deviations across the three seeds are $0.004$--$0.021$ except at the two pure-Adam
CReLU cells, where they reach $0.058$--$0.070$.

\begin{figure}[H]
\centering
\includegraphics[width=0.49\textwidth]{switch_relu.pdf}
\hfill
\includegraphics[width=0.49\textwidth]{switch_crelu.pdf}
\caption{Test accuracy against the handover step, both directions, ReLU (left) and CReLU (right).
The endpoints of each curve reproduce the unmixed optimisers and serve as internal checks.}
\label{fig:switch}
\end{figure}

\paragraph{Conclusions.}
The decisive comparison holds Adam's budget \emph{fixed} at $6000$ steps and varies only when it
runs. Adam first gives $0.606$ (ReLU) and $0.609$ (CReLU); Adam last gives $0.547$ and $0.503$. Same
optimiser, same number of steps, $0.06$--$0.11$ of accuracy from position alone.

The asymmetry is sharp in both directions. Three thousand reference steps followed by nine thousand
Adam steps reaches only $0.562$ (ReLU) against $0.645$ for pure Adam: \emph{Adam cannot undo what
the reference does early, even with $75\%$ of the budget afterwards}. Conversely, handing over
\emph{to} the reference is cheap --- Adam for $6000$ then the reference for $6000$ gives $0.606$,
only $0.04$ below pure Adam. The reference can largely hold what Adam built; it cannot build it.

On CReLU the mixture is the best cell in the table: Adam for $3000$ steps then the reference for
$9000$ gives $0.660 \pm 0.008$, against pure reference $0.561$ and pure Adam $0.596 \pm 0.070$. We
do \emph{not} claim it beats pure Adam. That cell's five seeds span $0.204$, so $0.596 + 0.070$
already reaches $0.666$ and the two intervals overlap. What the comparison does support is the
claim against the pure \emph{reference}, which is $0.099$ lower with a seed spread of $0.076$, and
the observation that the mixture's own spread is an order of magnitude tighter than pure Adam's.
Settling whether the mixture genuinely exceeds pure Adam needs more seeds on that one cell.

This is the clearest positive result in Part II and it sharpens the open question: what does Adam do
in the first few thousand steps that the reference does not, and why is it irreversible?

% ---------------------------------------------------------------------------
\subsubsection{E14 --- Extended training}
\label{sec:e14}

\paragraph{Motivation and question.}
All arms are demonstrably still moving at $12000$ steps. \emph{Does the ordering survive $20$--$80
\times$ longer training, and does the reference's disadvantage close if it is simply given more
time?} This experiment is the deliberate study of the regime that the convergence-phase estimator
excludes everywhere else: every number here is read long after every arm has fit the training set,
which is the point. It is reported as a trajectory rather than a single window for that reason.

\paragraph{Setup.}
ReLU, seed $0$, each arm continued from its $12000$-step checkpoint to $300000$ steps (and Adam to
$10^6$), with snapshots every $25000$ steps. The original runs are untouched; the continuations are
separate.

\paragraph{Measurement.}
Test accuracy, training loss, gate churn, switching fraction, linear-region count on a fixed data
plane, region concentration, operator rank, and adversarial accuracy at each snapshot.

\paragraph{Status.} \textsc{done}.

\paragraph{Results.}

\begin{center}\small
\begin{tabular}{lccccc}
\toprule
arm & test acc. & regions & switching & operator rank & gate churn \\
 & $25$k $\to$ $300$k & $25$k $\to$ $300$k & $25$k $\to$ $300$k & $25$k $\to$ $300$k & late \\
\midrule
gradient descent & $0.529 \to 0.527$ & $56.9$k $\to 56.2$k & $0.993 \to 0.992$ & $5.68 \to 5.72$ & $10^{-4}$ \\
Adam             & $0.639 \to 0.477$ & $43.0$k $\to 26.3$k & $0.925 \to 0.614$ & $2.14 \to 1.00$ & $2\cdot10^{-2}$ \\
reference        & $0.526 \to 0.496$ & $34.6$k $\to 33.1$k & $0.694 \to 0.655$ & $1.28 \to 1.02$ & $2\cdot10^{-4}$ \\
\bottomrule
\end{tabular}
\end{center}

\begin{figure}[H]
\centering
\includegraphics[width=\textwidth]{fig_extended_relu_all.pdf}
\caption{Every tracked statistic over $300000$ steps, all three arms on shared axes. Gradient
descent is static; only Adam keeps restructuring, and it eventually destabilises.}
\label{fig:extended}
\end{figure}

\begin{figure}[H]
\centering
\includegraphics[width=0.49\textwidth]{spikes_adam_relu.pdf}
\hfill
\includegraphics[width=0.49\textwidth]{slice_evolution_adam_relu.pdf}
\caption{Left: loss spikes along Adam's extended run and the gate churn that accompanies them.
Right: the same data plane at successive snapshots.}
\label{fig:spikes}
\end{figure}

\paragraph{Conclusions.}
These are single-seed observations. The effects below are large relative to the seed spreads seen
elsewhere in Part II ($0.010$--$0.070$ in test accuracy), but continuing three seeds to $300$k
steps was not affordable, so each statement should be read as one trajectory rather than an
average.

\emph{Gradient descent is frozen.} Over $275000$ steps past convergence, test accuracy moves by
$0.002$, linear regions by $-1.2\%$, switching by $0.001$ and operator rank by $0.04$. Whatever
gradient descent was going to do, it has done by step $25000$.

The reference is nearly as static \emph{over this window}, but the window begins long after its
own collapse: its operator rank is $6.99$ at the convergence phase (step $800$),
$1.58$ at step $12000$, and $1.28$ already by step $25000$ where this table starts. The
$1.28 \to 1.02$ it moves over the next $275000$ steps is the tail of that collapse, not a separate
phenomenon, and reading Section~\ref{sec:e9} together with this row gives the full arc.

Only Adam keeps restructuring --- regions fall by $39\%$ and
switching by a third --- and the restructuring does not pay: its best accuracy over the whole
extended run ($0.660$ at $100$k) is $0.02$ above its $12$k value, and by $300$k it has destabilised,
with training loss climbing from $10^{-9}$ to $0.69$ and test accuracy falling to $0.477$. The
$10^6$-step continuation collapses to chance.

No grokking occurs. We note the regime is one in which classical grokking is not predicted: no
weight decay and standard-scale initialisation \citep{liu2023omnigrok}. A null result here is
therefore uninformative about grokking rather than evidence against it.

Loss spikes coincide with gate reorganisation: $104$ spikes detected along Adam's run, with gate
churn six times higher during them ($0.0348$ against $0.0058$) and test accuracy unchanged across
them ($0.620$ against $0.612$). These are reorganisation events rather than damage.

The headline answer to the question is negative: more time does not close the reference's gap, and
the ordering does not reverse.

% ---------------------------------------------------------------------------
\subsubsection{E15 --- Isolating the momentum effect}
\label{sec:e15}

\paragraph{Motivation and question.}
The deflection diagnostic of E4 compares a \emph{single} step against the request made at that same
point. That is the right comparison for a memoryless rule, but it is systematically unkind to a rule
with memory. Adam carries a first-moment average with $\beta_1 = 0.9$, so its current step is a
weighted average of roughly the last ten gradients; it therefore \emph{must} deviate from the
current request, not because its direction is wrong but because it is integrating. Its near-zero
$\cos_{op}$ is thus not, on its own, evidence about the quality of the direction it takes.

This raises a concrete possibility that the diagnostic cannot see: \emph{Adam may be performing
something equivalent to momentum at the operator level --- accumulating a coherent operator motion
across steps --- while every individual step scores as unfaithful.} If so, the framework would be
missing the mechanism entirely rather than measuring it.

The question this experiment answers is therefore: \emph{is momentum the mechanism?} Adding momentum
directly to the operator step isolates that one ingredient, holding target, solver, step size and
seeds fixed. If accumulated operator motion is what Adam's advantage rests on, the operator rule
with momentum should capture it.

\paragraph{Setup.}
The reference step is smoothed directly,
\begin{equation}
  u_t = \beta\, u_{t-1} + (1-\beta)\, \Delta W^{\mathrm{ref}}_t, \qquad W_{t+1} = W_t + u_t ,
  \label{eq:opmom}
\end{equation}
with \emph{no} learning rate: the operator rule has none, since the step size enters through $\eta$
inside $\Delta_\star$. The factor $(1-\beta)$ keeps $\|u_t\|$ on the scale of a single reference
step, so $\beta$ changes the direction the step is smoothed towards and not its magnitude, and
$\beta = 0$ recovers the unmodified reference exactly. This is momentum on the \emph{realised
operator displacement} rather than on the gradient, which is what makes it the operator-level
analogue of Adam's first moment. ReLU and CReLU, $\eta \in \{0.5, 1\}$, $k = 50$,
$\beta \in \{0, 0.5, 0.9, 0.99\}$, three seeds: $48$ cells.

\paragraph{Measurement.}
Test accuracy at the convergence phase, paired against the $\beta = 0$ control on the same seed,
together with $\cos_{op}$ over the same cells, the $\ker M$ fraction of the resulting step, and the
training loss.

\paragraph{Status.} \textsc{done}.

\paragraph{Results.} Paired difference from the $\beta = 0$ control, three seeds:

\begin{center}\small
\begin{tabular}{llccc}
\toprule
architecture & $\eta$ & $\beta = 0.5$ & $\beta = 0.9$ & $\beta = 0.99$ \\
\midrule
ReLU  & $0.5$ & $-0.004 \pm 0.005$ & $-0.017 \pm 0.007$ & $-0.020 \pm 0.010$ \\
      & $1$   & $-0.013 \pm 0.009$ & $-0.006 \pm 0.009$ & $\mathbf{-0.075 \pm 0.027}$ \\
CReLU & $0.5$ & $-0.006 \pm 0.008$ & $-0.019 \pm 0.010$ & $-0.010 \pm 0.010$ \\
      & $1$   & $-0.014 \pm 0.011$ & $-0.021 \pm 0.016$ & $\mathbf{-0.037 \pm 0.007}$ \\
\bottomrule
\end{tabular}
\end{center}

\noindent All twelve cells are negative. Read at the end of the budget instead, three of them are
slightly positive ($+0.004$, $+0.006$, $+0.003$), which is within their seed spread and was the
only thing standing between this experiment and a clean result; at matched phase it is uniform.

\noindent Over the same cells $\cos_{op}$ falls monotonically from $0.80$--$0.88$ at $\beta = 0$ to
$0.05$--$0.12$ at $\beta = 0.99$. Every cell still interpolates.

\paragraph{Momentum moves the step out of $\mathrm{range}(M^\top)$, and it does so identically for
both rules.} The $\ker M$ fraction of Section~\ref{sec:e4} is exactly zero for the reference,
because $M^{+}\Delta_\star$ lies in $\mathrm{range}(M^\top)$ by construction. Adding momentum
destroys that, for a reason with nothing to do with the rule: the buffer is a sum of past steps,
each of which lay in $\mathrm{range}(M^\top)$ \emph{at the moment it was formed}, and that subspace
rotates as the weights move. Measured on ReLU with each rule's own state warmed for $300$ steps
from its convergence snapshot:

\begin{center}\small
\begin{tabular}{lccccc}
\toprule
operator momentum, $\beta$ & $0$ & $0.5$ & $0.9$ & $0.99$ & \\
\midrule
$\ker M$ fraction & $0.0000$ & $0.211$ & $0.596$ & $0.416$ & \\
\midrule
heavy ball, learning rate & --- & $3\cdot10^{-3}$ & $10^{-2}$ & $3\cdot10^{-2}$ & (gd: $0$) \\
\midrule
$\ker M$ fraction & --- & $0.322$ & $0.422$ & $0.478$ & $0.0000$ \\
\bottomrule
\end{tabular}
\end{center}

\noindent The two rules behave the same way: zero without momentum, a third to six tenths with it,
at comparable $\beta$. Heavy ball's fraction also grows with its learning rate, which is the same
mechanism seen from the other side --- larger steps rotate $\mathrm{range}(M^\top)$ faster, so a
buffer of given length drifts further out of it.

This sharpens the experiment rather than explaining it away. Heavy ball and operator momentum put
comparable fractions of their step outside $\mathrm{range}(M^\top)$, and heavy ball gains $0.111$
over gradient descent while operator momentum \emph{loses} against the plain reference in all
twelve cells. Whatever makes momentum work in weight space, it is therefore not that momentum
carries the step off the manifold of function-changing directions: both rules do that, to the same
degree, and only one of them benefits.

\begin{figure}[H]
\centering
\includegraphics[width=\textwidth]{fig_momentum.pdf}
\caption{Operator momentum: test accuracy against $\beta$ (left column) and single-step
operator-space deflection over the same cells (right column), for ReLU and CReLU. Momentum lowers
the single-step deflection by an order of magnitude and buys nothing.}
\label{fig:momentum}
\end{figure}

\paragraph{Conclusions.}
\emph{Momentum is not the mechanism.} No $\beta$ beats the $\beta = 0$ control by more than noise,
and $\beta = 0.99$ costs $0.05$--$0.07$ where the effect is largest. Since every cell interpolates,
this is a generalisation effect and not a failure to fit. Accumulating operator motion across steps,
which is the operator-level analogue of what Adam's first moment does, does not reproduce Adam's
advantage. Whatever Adam is doing, it is not that.

\emph{The measurement caveat is confirmed and quantified.} Momentum does exactly what the framing
predicted it would do to the diagnostic: it drives single-step $\cos_{op}$ down by an order of
magnitude, from $0.80$--$0.88$ to $0.09$--$0.30$, purely by carrying stale displacement. The reason
is structural --- $\Delta W^{\mathrm{ref}}_t$ lies in $\mathrm{range}(M_t^\top)$, but $M_t$ moves
with the weights, so a stale $u_{t-1}$ has a component in $\ker M_t$ at the current point. So a low
single-step deflection is genuinely ambiguous between "this rule takes a poor direction" and "this
rule has memory", and Adam's near-zero value cannot be read as the former without this control.

The two findings together close the loop. Momentum \emph{can} produce a low $\cos_{op}$ without any
benefit, so the diagnostic's unkindness to rules with memory is real; and momentum applied to the
operator rule produces no benefit either, so the hypothesis that Adam's advantage is hidden
accumulated operator motion is eliminated rather than merely unmeasured. A useful consequence for
reading the rest of Part II: any conclusion drawn from $\cos_{op}$ about a momentum-carrying rule
should be understood as a statement about its single steps, not about its trajectory.

\subsubsection{E16 --- Adversarial robustness}
\label{sec:e16}

\paragraph{Motivation and question.}
Clean accuracy is one axis. \emph{Do the arms differ in the robustness of the function they arrive
at, and if so does that separate them where the structural measures did not?} A method that places
its partition on the data (E11) and under-uses its gating (E8) might plausibly produce a flatter,
more robust function.

\paragraph{Setup.}
Every $(\text{architecture}, \text{arm}, \text{hyper-parameter})$ cell at the common configuration,
up to three seeds, scored at the final checkpoint with no selection of any kind.

\paragraph{Measurement.}
Accuracy under an $L_\infty$-bounded attack of radius $\epsilon$, $50$ steps of size
$2.5\epsilon/50$ from a uniform random start, two restarts. The attack ascends the margin
$\max_{j \ne y} z_j - z_y$ rather than the cross-entropy. This choice is essential rather than
incidental: the networks here are bias-free and positively homogeneous, so nothing bounds the logit
scale along an interpolating trajectory, and the measured logit margins differ between arms by two
orders of magnitude ($0.78$ to $14.1$ in the table below). A cross-entropy attack saturates against
large-margin networks and reports them as robust; the margin objective is scale-covariant, so
multiplying all logits by $c > 0$ multiplies it by $c$ and leaves its gradient direction unchanged.
We report robustness as a \emph{ratio} to clean accuracy, since the arms differ by up to $13$ points
of clean accuracy.

\paragraph{Status.} \textsc{done}.

\paragraph{Results.} Best hyper-parameter per arm, interpolating cells only, at the convergence
phase; the endpoint value follows in parentheses wherever the two differ by more than a rounding:

\begin{center}\small
\begin{tabular}{llcccccc}
\toprule
architecture & arm & clean & $\epsilon{=}0.05$ & $\epsilon{=}0.1$ & $\epsilon{=}0.3$ & margin & $\|P\|_F$ \\
\midrule
ReLU  & gradient descent & $0.531$ & $58.5\%$ & $32.1\%$ & $0.6\%$ & $1.41$ & $43.8$ \\
      & Adam             & $0.642$ & $\mathbf{65.8\%}$ & $\mathbf{40.7\%}$ & $\mathbf{3.7\%}$ & $9.53$ & $80.2$ \\
      & reference        & $0.570$ & $63.2\%$ & $37.8\%$ & $2.7\%$ & $3.43$ & $40.9$ \,($374.7$) \\
\midrule
CReLU & gradient descent & $0.517$ & $57.0\%$ & $30.9\%$ & $1.2\%$ & $0.67$ & $32.1$ \\
      & Adam             & $0.572$ & $46.5\%$ & $22.5\%$ & $0.3\%$ & $6.30$ & $203.1$ \\
      & reference        & $0.600$ & $\mathbf{65.5\%}$ & $\mathbf{41.2\%}$ & $\mathbf{3.3\%}$ & $8.70$ & $82.9$ \,($1002.9$) \\
\midrule
leaky & gradient descent & $0.529$ & $59.0\%$ & $32.0\%$ & $1.0\%$ & $1.34$ & $44.9$ \\
      & Adam             & $0.655$ & $\mathbf{65.9\%}$ & $39.8\%$ & $2.4\%$ & $6.76$ & $43.3$ \\
      & reference        & $0.595$ & $65.2\%$ & $\mathbf{42.2\%}$ & $\mathbf{3.6\%}$ & $5.54$ & $63.6$ \,($243.2$) \\
\bottomrule
\end{tabular}
\end{center}

\noindent The robustness ratios themselves barely move between the two windows --- no entry shifts
by more than $1.5$ points --- so this measure, like switching and region concentration, is stable
to the choice of window. The operator-norm column is the exception and it moves by an order of
magnitude: the reference's $\|P\|_F$ is $40.9$, $82.9$, $63.6$ at the convergence phase against
$374.7$, $1002.9$, $243.2$ at the endpoint. Read at convergence the reference's operator is
\emph{not} unusually large --- it is comparable to gradient descent's and smaller than Adam's on
CReLU --- and the earlier reading that the reference arrives at an operator five to twenty times
the norm of the baselines' was measuring the post-interpolation drift of
Remark~\ref{rem:normalise}, not the solution. This matters for the regression below, which uses
$\|P\|_F$ as a predictor.

\begin{figure}[H]
\centering
\includegraphics[width=\textwidth]{fig_robustness.pdf}
\caption{Both panels at the convergence phase. Left: adversarial accuracy as a fraction of clean
accuracy against $\epsilon$, best hyper-parameter per arm (solid ReLU, dashed CReLU, dotted leaky);
the curves lie on top of each other. Right: the robustness ratio at $\epsilon = 0.1$ against the
logit margin across every interpolating cell, with the fitted line. The margin accounts for the
variation ($\rho = +0.87$); operator rank does not ($\rho = +0.04$, and $-0.63$ if the same cells
are read at the end of the budget instead).}
\label{fig:robustness}
\end{figure}

\paragraph{Conclusions.}
There is no separation between the arms. As a fraction of clean accuracy they lie within a few
points of each other at every radius in two of three architectures --- $57$--$66\%$ at
$\epsilon = 0.05$, $31$--$42\%$ at $\epsilon = 0.1$ --- and the ordering is not consistent across
architectures. The one cell that stands out is CReLU-Adam, which is the least robust of the nine at
every radius ($46.5\%$ at $\epsilon = 0.05$ against $57.0\%$ and $65.5\%$ for the other two arms)
while being neither the best nor the worst on clean accuracy. Robustness adds nothing to the
separation of the arms, and this experiment is reported as another elimination.

The $\|P\|_F$ column no longer supports the reading we previously drew from it, and the retraction
is worth stating because it is a case where the measurement window changed a conclusion rather than
a number. Since $f(x) = P(x)x$ here, $\|P\|_F$ \emph{is} the input-Jacobian norm. At the endpoint
the reference's is $4$--$20\times$ larger than gradient descent's, which we read as ruling out
Jacobian-norm regularisation as its mechanism. At the convergence phase it is $40.9$, $82.9$ and
$63.6$ against gradient descent's $43.8$, $32.1$ and $44.9$ --- the same order of magnitude, larger
on two architectures and indistinguishable on the third. The reference is still not
\emph{minimising} the Jacobian norm, so the mechanism is still ruled out
\citep{jakubovitz2018jacobian}; but the dramatic version of that claim was measuring the
post-interpolation drift of Remark~\ref{rem:normalise}.

\paragraph{A correlation that does not survive the window.}
Across interpolating cells, operator rank correlates with robustness at $\rho = -0.63$ when both
are read at the end of the budget --- the observation that prompted the suggestion, in an earlier
version of this work, that constraining operator rank might buy robustness. Read at the convergence
phase the same correlation over the same $61$ cells is $\rho = +0.04$. It is not weakened; it is
gone, and its sign is not even determined.

The mechanism of the spurious version is visible in the other columns. Over the post-interpolation
tail the reference's rank falls and its operator norm rises together --- $\rho(\log\|P\|_F,
\text{robustness}) = +0.62$ at the endpoint --- so cells with a long tail acquire both a low rank
and a high robustness ratio, and rank inherits a correlation that belongs to the drift. At
convergence that channel is closed and the norm correlation drops to $+0.27$.

What does predict robustness, in both windows and strongly, is the logit margin:
$\rho = +0.87$ at the convergence phase and $+0.77$ at the endpoint. This is the expected result
rather than a finding --- the CW objective is scale-covariant, so a larger margin is a larger
distance to the decision boundary in the units the attack works in --- but it accounts for the
robustness variation across cells without reference to the operator at all. We therefore draw no
conclusion linking operator rank to robustness, and withdraw the earlier suggestion that one
exists.

% ---------------------------------------------------------------------------
\subsubsection{E17 --- Behaviour against depth}
\label{sec:e17}

\paragraph{Motivation and question.}
The framework is motivated by the claim that the gap between a weight step and the operator step it
induces \emph{grows with depth}. Every experiment above is at a single depth, so the claim is so far
untested here. \emph{Does operator-space faithfulness decay with depth; does each arm still reach
interpolation as depth grows; and does the generalisation ordering survive?}

\paragraph{Setup.}
$L \in \{2, 4, 8, 16, 32\}$, ReLU and CReLU, width $128$, $12000$ steps, three seeds. Learning-rate
grids extended downward ($10^{-4}$ for Adam, $10^{-3}$ for gradient descent) because the useful
range moves with depth; $\eta \in \{0.3, 1, 3\}$ at $k = 50$ for the reference. $390$ cells.

\paragraph{Measurement.}
Per depth and arm, at the best hyper-parameter by the convergence-phase estimator: test accuracy,
training accuracy and training loss over that window --- so that a change in test accuracy can be
attributed to generalisation rather than to a failure to fit --- and $\cos_{op}$ at the first
alignment probe of the same window.

\paragraph{Status.} \textsc{done} at $k = 50$: both architectures, three seeds, every depth. The
$k = 200$ retrain at $L = 32$ is complete on ReLU at three seeds. A $k = 500$ spot check was
started and stopped at $31\%$ of its single cell in favour of CIFAR-10; the $k = 200$ result below
rests on three seeds and does not depend on it.

\paragraph{Results.} Test accuracy at each arm's best hyper-parameter over its whole grid, three
seeds, with the highest training accuracy that cell reaches in parentheses wherever it falls short
of $0.999$:

\begin{center}\small
\begin{tabular}{llccccc}
\toprule
 & & $L=2$ & $L=4$ & $L=8$ & $L=16$ & $L=32$ \\
\midrule
ReLU  & gradient descent & $0.538$\,{\scriptsize(.867)} & $0.558$ & $0.527$ & $\mathbf{0.579}$ & $0.505$ \\
      & Adam             & $\mathbf{0.545}$ & $0.607$\,{\scriptsize(.993)} & $\mathbf{0.645}$ & $0.635$\,{\scriptsize(.994)} & $0.472$\,{\scriptsize(.989)} \\
      & reference        & $0.506$ & $\mathbf{0.587}$ & $0.592$ & $0.567$ & $0.441$ \\
      & \; at $k = 200$  & ---     & ---     & ---     & ---     & $\mathbf{0.520}$ \\
\midrule
CReLU & gradient descent & $0.533$\,{\scriptsize(.933)} & $0.572$ & $0.517$ & $0.532$ & $0.479$ \\
      & Adam             & $\mathbf{0.543}$\,{\scriptsize(.907)} & $\mathbf{0.676}$\,{\scriptsize(.996)} & $0.596$ & $\mathbf{0.645}$ & $\mathbf{0.528}$ \\
      & reference        & $0.516$ & $0.578$ & $\mathbf{0.597}$ & $0.560$ & $0.500$ \\
\bottomrule
\end{tabular}
\end{center}

\noindent Restricted instead to cells that fit --- the convergence-phase reading used elsewhere in
Part II --- the ReLU row for Adam becomes $0.546$, $0.589$, $0.642$, $0.552$, $0.322$ and the other
two rows move by at most $0.02$. The difference is confined to Adam at $L \ge 16$, and it is the
whole of the depth story for that arm, so both readings are given below rather than one being
preferred.

\noindent and operator-space faithfulness over the same cells:

\begin{center}\small
\begin{tabular}{llccccc}
\toprule
$\cos_{op}$ & & $L=2$ & $L=4$ & $L=8$ & $L=16$ & $L=32$ \\
\midrule
ReLU  & gradient descent & $0.398$ & $0.195$ & $0.213$ & $0.136$ & $0.068$ \\
      & Adam             & $0.230$ & $0.112$ & $0.035$ & $0.047$ & $0.013$ \\
      & reference ($k{=}50$) & $1.000$ & $0.796$ & $0.670$ & $0.656$ & $0.779$ \\
\midrule
CReLU & gradient descent & $0.423$ & $0.290$ & $0.245$ & $0.286$ & --- \\
      & Adam             & $0.210$ & ---     & $0.040$ & $0.023$ & --- \\
      & reference ($k{=}50$) & $1.000$ & $0.722$ & $0.948$ & $0.673$ & --- \\
\bottomrule
\end{tabular}
\end{center}

\begin{figure}[H]
\centering
\includegraphics[width=\textwidth]{fig_depth_summary.pdf}
\caption{Depth sweep: test accuracy, training accuracy, training loss and $\cos_{op}$ against
depth, all read at the convergence phase, at each arm's best hyper-parameter by the same estimator.
A point is drawn only where all three seeds of that cell have a convergence phase, so the gaps are
cells that are incomplete or that did not fit rather than points that would move later.
\textsc{partial}: CReLU at $L = 32$ is still running.}
\label{fig:depthsummary}
\end{figure}

\paragraph{Interim conclusions.}
\emph{The mismatch grows with depth.} Gradient descent's faithfulness falls monotonically from
$0.398$ at $L=2$ to $0.068$ at $L=32$ on ReLU and from $0.423$ to $0.168$ on CReLU, and Adam's from
$0.230$ to $0.013$ over the same range. This is the framework's motivating claim appearing in a
measurement, and it is why the trajectory figure of Section~\ref{sec:e2} is drawn at $L = 32$.

\emph{The reference's advantage over gradient descent peaks at $L=8$ and decays to zero, but does
not reverse.} At the convergence phase the margin (reference minus gradient descent) runs

\begin{center}\small
\begin{tabular}{lccccc}
\toprule
margin, reference $-$ gd & $L=2$ & $L=4$ & $L=8$ & $L=16$ & $L=32$ \\
\midrule
ReLU, $k = 50$    & $-0.018$ & $+0.033$ & $\mathbf{+0.067}$ & $-0.018$ & $-0.063$ \\
ReLU, $k = 200$   & ---      & ---      & ---               & ---      & $-0.002$ \\
CReLU, $k = 50$   & $+0.002$ & $+0.019$ & $\mathbf{+0.088}$ & $+0.025$ & $-0.004$ \\
\bottomrule
\end{tabular}
\end{center}

\noindent absent when shallow, largest at $L=8$ --- the depth at which all of Part II is run ---
and gone by $L=16$. The $3$--$9$ point margin reported in E5 is therefore a property of a
particular depth rather than a trend, and the framework's motivating claim --- that the
weight/operator mismatch grows with depth --- does \emph{not} translate into a growing benefit
from correcting it. That is the sharpest negative result in the paper, because it is the one the
framework would most have liked to be otherwise: the diagnostic behaves exactly as predicted while
the intervention it suggests does not.

We previously reported the $L=32$ ReLU margin as $-0.063$ and read it as the reference being
actively \emph{worse} than gradient descent at depth. That was a truncation artefact and is
withdrawn. At $L=32$ the reference arm running at $k=50$ achieves $\cos_{op} = 0.78$, so it is not
taking the reference step but a substantially deflected approximation to it; retrained at
$k = 200$, where $\cos_{op}$ rises to $0.85$, the same cells go from $0.445$ to $0.505$ against
gradient descent's $0.507$. The correct statement is that the benefit decays to nothing with
depth, not that it reverses. CReLU makes the same point from the other direction: its step map is
better conditioned, $k = 50$ already reaches $\cos_{op} = 0.96$ at $L=32$, and its margin there is
$-0.004$ without any increase in $k$.

\emph{A consequence for anyone using this construction.} The truncation $k$ is not a free constant
that can be fixed once: at $L=8$ on ReLU, $k=50$ delivers $\cos_{op} = 0.67$, and at $L=32$ it
delivers $0.78$ only at the best $\eta$ and $0.69$ at others, so a depth sweep run at fixed $k$
compares the reference against itself at varying fidelity. Every cross-depth comparison in this
section should be read with the $\cos_{op}$ table beside it, and a study that wanted the reference
proper at every depth would have to scale $k$ with depth --- at a cost, since $k = 200$ at $L = 32$
takes $3080$\,s per run against $942$\,s at $k = 50$.

\emph{At $L=32$ the ordering inverts, and Adam's problem is that fitting and generalising stop
being available at the same learning rate.} On ReLU the ordering becomes gradient descent $(0.505)$
$>$ Adam $(0.470)$ $>$ reference $(0.442)$, against Adam first at every depth up to $8$. Adam's
learning-rate grid at this depth separates cleanly into cells that fit and cells that generalise,
and they are disjoint:

\begin{center}\small
\begin{tabular}{lcccc}
\toprule
Adam at $L=32$, ReLU & $10^{-4}$ & $3\cdot10^{-4}$ & $10^{-3}$ & $3\cdot10^{-3}$, $10^{-2}$ \\
\midrule
test accuracy         & $0.319$ & $0.421$ & $\mathbf{0.470}$ & --- \\
max training accuracy & $1.000$ & $0.997$ & $0.989$ & --- \\
final training loss   & $2.6\cdot10^{-6}$ & $1.5\cdot10^{-2}$ & $5.7\cdot10^{-1}$ & non-finite \\
\bottomrule
\end{tabular}
\end{center}

\noindent The only cell that fits is the smallest learning rate, and it is also the worst on test;
the two largest diverge outright in all three seeds. So Adam at $L = 32$ is not simply worse --- it
no longer has a setting that both fits and generalises, and which number one quotes for it
($0.470$ or $0.322$) depends entirely on whether fitting is required. Gradient descent (loss
$1.4\cdot10^{-5}$) and the reference (loss $1.0\cdot10^{-5}$) have no such problem and both
interpolate comfortably.

\emph{One caveat on the shallow end.} At $L=2$ the arms are slow rather than unable: every arm
reaches training accuracy $\ge 0.999$, but only at step $4300$--$11500$, and at training losses of
$4\cdot10^{-3}$ to $7\cdot10^{-2}$ --- one to two orders of magnitude looser than at $L=4$. That
column is a matched-\emph{phase} comparison but a weak matched-fit one, and the near-tie it shows
should be read with that in mind.

The reference's own $\cos_{op}$ row requires the reading established in E4: it is $1.000$ at $L=2$,
where fifty Krylov iterations fully converge, and lower at greater depth because a deeper step map
needs more iterations at fixed $k$. It does not decay monotonically with depth beyond that --- on ReLU it sits at
$0.67$, $0.66$, $0.78$ at $L = 8, 16, 32$ --- so the row measures how far fifty Krylov iterations
get, which depends on the conditioning of the step map rather than on depth alone. The important
consequence is that at $L \ge 8$ the operator arm is \emph{not} running the reference step: it is
running a $30$--$50\%$ deflected approximation to it, and the deficit is largest exactly where the
arm performs worst. Measured directly at $L = 32$ on ReLU, the operator arm's
$\cos_{op}$ against a converged target rises from $0.71$ at $k = 50$ to $0.88$ at $k = 200$,
$0.978$ at $k = 1000$ and $0.9999$ at $k = 3000$. That row is a statement about the truncation, not
about the rule.

\emph{Which makes the $L = 32$ result provisional, and we mark it as such.} The reference arm at
$L = 32$ is trained at $k = 50$, where its step is only $\approx 0.7$--$0.8$ aligned with the step
it is supposed to be taking. Its being beaten by gradient descent there is therefore not yet
evidence that the reference step is worse at depth; it is equally consistent with fifty Krylov
iterations being too few to realise the reference step at this depth. The two readings are
distinguishable by experiment --- retraining the $L = 32$ ReLU cells at larger $k$ --- and that run
is in progress. Until it reports, the honest form of the depth conclusion is that \emph{the
reference as we can afford to compute it} loses to gradient descent at $L = 32$, and the margin
figures above should be read with the $k$ column beside them.

\subsubsection{E18 --- Preconditioned and orthogonalised comparators}
\label{sec:e18}

\paragraph{Motivation and question.}
The reference step is a preconditioner, $-\eta (M^\top M)^{+}\nabla_W \mathcal{L}$, and the natural
objection is that it may be a known one in disguise. \emph{How does it relate empirically to the
geometry-aware optimisers of Section~\ref{sec:sota}?} K-FAC is the sharpest case: in the
isotropic deep-linear setting it diagonalises in the same bases as the reference and differs only in
its denominator, $(\sigma_A^2 + \lambda)(\sigma_B^2 + \lambda)$ against
$\sigma_A^2 \sigma_B^2 + \lambda$, so it is predicted to be the closest baseline and to depart from
the projection precisely in the high-damping regime used for stability at depth.

\paragraph{Setup.}
Muon, Shampoo, SOAP, K-FAC and heavy ball, added as arms to the common configuration at $L = 8$,
ReLU and CReLU, three seeds, over per-optimiser learning-rate grids. Each is implemented to match
its published form; K-FAC reads its Kronecker factors off the layer chain rather than estimating
them in parameter space, which is what makes the prediction above testable.

\paragraph{Measurement.}
The full diagnostic set of E4 and E5: $\cos_{op}$, $\cos_w$, the $\ker M$ fraction, operator rank
and test accuracy, all at the convergence phase, so that each comparator takes its place in the same table as the three
main arms.

\paragraph{Status.} \textsc{partial}. Heavy ball, Muon, SOAP and Shampoo are complete at three
seeds on both architectures. K-FAC is being re-run after the implementation fault described below,
so its columns are marked and its rows withheld. Muon's and SOAP's grids were extended downward,
for the reason the table makes visible.

\paragraph{Results.} Best \emph{fitting} cell per optimiser --- one cell per column, every row read
from it at the convergence phase:

\begin{center}\small
\begin{tabular}{lcccccccc}
\toprule
ReLU & heavy ball & Adam & K-FAC$^{\ast}$ & reference & gd & Shampoo & Muon & SOAP \\
\midrule
test accuracy & $\mathbf{0.642}$ & $0.634$ & $0.615$ & $0.556$ & $0.531$ & $0.513$ & $0.475$ & $0.458$ \\
lr / $\eta$ & $0.03$ & $0.003$ & $0.003$ & $0.5$ & $0.1$ & $0.1$ & $0.001$ & $0.001$ \\
\midrule
$\cos_{op}$ & $0.150$ & $0.032$ & $0.256$ & $\mathbf{0.941}$ & $0.228$ & $0.273$ & $0.039$ & $0.082$ \\
$\cos_w$ & $0.013$ & $-0.002$ & $0.045$ & $0.571$ & $0.122$ & $0.169$ & $0.023$ & $0.017$ \\
$\alpha$ & $0.996$ & $0.770$ & $1.000$ & $1.000$ & $1.000$ & $1.000$ & $1.000$ & $1.000$ \\
operator rank & $3.70$ & $\mathbf{2.05}$ & $4.19$ & $7.42$ & $5.68$ & $6.39$ & $6.99$ & $7.18$ \\
gate density & $0.316$ & $\mathbf{0.221}$ & $0.354$ & $0.611$ & $0.488$ & $0.524$ & $0.550$ & $0.559$ \\
gate diversity & $0.410$ & $0.290$ & $0.440$ & $0.264$ & $0.425$ & $0.388$ & $0.345$ & $0.341$ \\
gate churn & $0.010$ & $0.021$ & $0.010$ & $0.032$ & $0.013$ & $0.010$ & $0.035$ & $0.022$ \\
dead units & $0.007$ & $0.120$ & $0.002$ & $0.101$ & $0.009$ & $0.015$ & $0.037$ & $0.024$ \\
\bottomrule
\end{tabular}
\end{center}

\begin{center}\small
\begin{tabular}{lcccccccc}
\toprule
CReLU & Adam & reference & heavy ball & K-FAC$^{\ast}$ & gd & Muon & SOAP & Shampoo \\
\midrule
test accuracy & $\mathbf{0.612}$ & $0.592$ & $0.587$ & $0.550$ & $0.504$ & $0.495$ & $0.491$ & $0.481$ \\
lr / $\eta$ & $0.003$ & $10$ & $0.01$ & $0.001$ & $0.03$ & $0.001$ & $0.001$ & $0.003$ \\
\midrule
$\cos_{op}$ & $0.034$ & $\mathbf{0.413}$ & $0.151$ & $0.320$ & $0.253$ & $0.072$ & $0.017$ & $0.290$ \\
$\cos_w$ & $0.066$ & $0.213$ & $0.163$ & $0.316$ & $0.272$ & $0.047$ & $0.002$ & $0.312$ \\
$\alpha$ & $1.000$ & $0.993$ & $1.000$ & $1.000$ & $1.000$ & $1.000$ & $1.000$ & $1.000$ \\
operator rank & $\mathbf{2.94}$ & $5.99$ & $6.01$ & $6.74$ & $6.61$ & $7.56$ & $7.58$ & $6.94$ \\
gate density & $0.498$ & $\mathbf{0.494}$ & $0.497$ & $0.499$ & $0.505$ & $0.502$ & $0.503$ & $0.509$ \\
gate diversity & $0.432$ & $0.309$ & $0.449$ & $0.425$ & $0.388$ & $0.364$ & $0.358$ & $0.324$ \\
gate churn & $0.037$ & $0.048$ & $0.014$ & $0.008$ & $0.009$ & $0.042$ & $0.029$ & $0.003$ \\
dead units & $0.004$ & $0.139$ & $0.000$ & $0.001$ & $0.003$ & $0.018$ & $0.020$ & $0.040$ \\
\bottomrule
\end{tabular}
\end{center}

\noindent$^{\ast}$ The K-FAC columns come from the faulty implementation described below and are
retained only so the table is not silently incomplete; they will be replaced. Every other column
is current.

\paragraph{Muon and SOAP do not have a setting that both fits and generalises.}
Their best cells by test accuracy are absent from the tables above because they never reach
training accuracy $0.999$: Muon's best reads $0.568$ on ReLU and $0.666$ on CReLU --- the highest
number anywhere in this experiment --- at a maximum training accuracy of $0.997$ and $0.995$, and
SOAP's reads $0.524$ and $0.608$ at $0.994$ and $0.985$. We extended both grids downward by two
learning rates to look for a cell that does both, and it does not exist: the fitting cells that
appeared score $0.475$ and $0.495$ for Muon and $0.458$ and $0.491$ for SOAP, below the
non-fitting ones in both cases. So this is not a grid artefact. These two methods, on this task,
reach their best generalisation at settings that stop short of fitting the training set, which is
the same fit/generalise split Adam shows at $L = 32$ in Section~\ref{sec:e17}.

\paragraph{Heavy ball is the result we did not expect.}
Plain gradient descent with momentum is the best optimiser in the table on ReLU, at $0.642$ against
Adam's $0.634$, fitting fully at three seeds and rising monotonically over its whole surviving
grid. It is also the cleanest control the experiment offers, because it reaches Adam's accuracy
\emph{without} Adam's diagnostics: operator rank $3.70$ against $2.05$, $\cos_{op}$ $0.150$ against
$0.032$, gate density $0.316$ against $0.221$, dead units $0.007$ against $0.120$. Whatever is
distinctive about Adam's endpoint on these measures is therefore not what its accuracy requires,
since a method matching that accuracy does not share it. This is a sharper version of the
elimination E9 and E10 arrive at.

\paragraph{Weight-space conditioning is not operator-space faithfulness.}
Muon orthogonalises its update, so it is perfectly conditioned in weight space by construction, and
it reads $\cos_{op} = 0.039$ on ReLU and $0.072$ on CReLU --- indistinguishable from Adam's
$0.032$ and $0.034$, and far below gradient descent's $0.228$ and $0.253$. Its $\ker M$ fraction,
$0.371$ and $0.529$, puts it with Adam on that measure too. Orthogonalising the step in the parameters does nothing
for the faithfulness of the operator step it induces. That is the distinction the paper rests on,
tested against a method built to be well conditioned in the other space, and it survives.

\paragraph{K-FAC is being re-run; no K-FAC number in this section is currently valid.}
\RUNNING{} An audit of the implementation found that the Fisher's $G$ factor was built from the
\emph{batch-mean} pre-activation gradient rather than the per-example one. Backpropagation through
a mean loss delivers $d/n$, so an implementation must undo the batch mean --- the reference
implementation this file was transcribed from writes \texttt{z\_grads[k] * B} with exactly that
comment, and the transcription dropped it while the call site divided by $n$ a second time. The
consequence is silent and severe. With $G$ smaller by $n^2 = 10^4$ and the damping a fixed
constant, the effective damping is $10^2$ too large, both Kronecker factors are replaced by
multiples of the identity, and the rule degenerates into rescaled gradient descent: measured, its
step sits at $\cos = 0.890$ to the plain gradient step, against $0.159$ once corrected.

Two conclusions we drew from the faulty runs are therefore withdrawn. K-FAC's accuracy of $0.615$
on ReLU is not K-FAC's. And its $\ker M$ fraction of $0.013$, sitting beside gradient descent's
$0.0000$, cannot be read as confirming the framework's prediction that a two-sided preconditioner
acts on the operator's geometry: gradient descent's step is $M^\top\Delta_\star$, which lies in
$\mathrm{range}(M^\top)$ by construction, so a rule that is $89\%$ gradient descent inherits a low
$\ker M$ fraction for free. The prediction about K-FAC is untested, not confirmed.

The learning-rate grid moves with the fix. Under the faulty scaling every rate from $10^{-2}$ to
$3\cdot10^{-1}$ diverged, which we read at the time as K-FAC being unstable at this depth;
corrected, nothing in that range diverges at all and the rule fits at $3\cdot10^{-3}$ and
$10^{-2}$. Both readings of the original grid were wrong for the same reason.

\emph{A second, earlier implementation fault in the solve.}
K-FAC's higher learning rates were failing with \texttt{torch.linalg.solve: singular matrix} a few
seconds into each run. That is not divergence and we initially misread it as such. The Kronecker
factors here are singular \emph{by construction}: $A_k = \frac{1}{n}h_{k-1}^\top h_{k-1}$ and
$G_k = \frac{1}{n}d_k^\top d_k$ are formed from a batch of $n = 100$ samples at width $128$, so
their rank is at most $100 < 128$, and the solve is well posed only because of the damping. The
factored Tikhonov split multiplies one factor's damping by $\pi$ and divides the other's by it, so
when $\pi$ drifts far from $1$ one factor is left effectively undamped and the solve fails. The fix
clamps $\pi$ and floors each factor's damping relative to that factor's own scale, and is covered
by a regression test that drives $\pi$ to both extremes across sixteen orders of magnitude of
residual scaling. Every K-FAC cell was re-run under the corrected solver, including those that had
completed under the old one, so that no row of this table mixes two implementations.

\paragraph{The $\ker M$ fraction measures memory, not adaptivity.}
$\ker M$ is the set of weight directions that leave the operator unchanged, so the fraction of a
step lying in it is the part of that step which provably does nothing to the function at the point
where it is taken. Measuring it requires care that an earlier version of this work did not take:
for a rule with memory the quantity depends on the rule's accumulated state, and scoring such a
rule from an empty state evaluates a different optimiser --- heavy ball with a zero momentum buffer
takes the plain gradient step, which lies in $\mathrm{range}(M^\top)$ by construction. Each rule
below is therefore measured with its own state, warmed for $300$ steps from its convergence-phase
snapshot on the batch stream its run used.

\begin{center}\small
\begin{tabular}{lcc|lcc}
\toprule
\multicolumn{3}{c|}{no accumulated direction} & \multicolumn{3}{c}{accumulated direction} \\
optimiser & ReLU & CReLU & optimiser & ReLU & CReLU \\
\midrule
gradient descent & $0.0000$ & $0.0000$ & Shampoo    & $0.352$ & $0.434$ \\
reference        & $0.0000$ & $0.0000$ & heavy ball & $0.478$ & $0.538$ \\
op.\ momentum, $\beta=0$ & $0.0000$ & $0.0000$ & Adam & $0.813$ & $0.561$ \\
K-FAC$^{\ast}$   & \RUNNING & \RUNNING & Muon       & $0.594$ & $0.747$ \\
                 &          &          & SOAP       & $0.841$ & $0.916$ \\
\bottomrule
\end{tabular}
\end{center}

\noindent The split is sharp --- nothing falls between $0.000$ and $0.352$ --- and it is not the
split one would guess. It does not separate adaptive from non-adaptive methods: heavy ball, which
has no adaptivity at all, sits at $0.478$ beside Adam. What the two groups differ in is whether the
rule carries an accumulated \emph{direction}: the left column is stateless, the right column all
accumulate a direction across steps. K-FAC is the informative test of that reading, because it
accumulates \emph{curvature} rather than a direction, and its row is pending.

The mechanism is visible in Section~\ref{sec:e15}: the momentum buffer is a sum of past steps, each
of which lay in $\mathrm{range}(M^\top)$ at the moment it was formed, and that subspace rotates as
the weights move. Operator momentum's fraction rises from exactly $0$ at $\beta = 0$ to $0.596$ at
$\beta = 0.9$, and heavy ball's rises with its learning rate, which is the same effect from the
other side. Three of the left-hand entries are zero \emph{by construction} --- gradient descent's
step is $M^\top\Delta_\star$ and the reference's is $M^{+}\Delta_\star$ --- and they measure
$0.0000$, which calibrates the projection. For gradient descent this recovers, as a measurement on
gated networks, the conservation law that gradient flow on homogeneous factored models does not
move along the layer-rescaling symmetries \citep{du2018algorithmic}.

\paragraph{The grouping does not predict generalisation.} Its two halves have mean accuracy $0.567$
and $0.540$, and each contains both a top method and a poor one: heavy ball at $0.642$ and SOAP at
$0.458$ on the right, K-FAC at $0.615$ and gradient descent at $0.504$ on the left. Over the
eighteen (architecture, optimiser) points the correlation with test accuracy is $-0.29$, and
$-0.21$ and $-0.39$ within the two architectures. So this measure, like $\cos_{op}$ in
Section~\ref{sec:e18b}, classifies optimisers sharply and says nothing about how well they
generalise --- and we note it explicitly because the three-arm version of this table invites the
opposite reading: gradient descent $0.000$ at $0.531$, reference $0.000$ at $0.565$, Adam $0.813$
at $0.634$ is monotone, and five further optimisers destroy it.

\paragraph{K-FAC is being re-run; no K-FAC number in this section is currently valid.}
\RUNNING{} An audit of the implementation found that the Fisher's $G$ factor was built from the
\emph{batch-mean} pre-activation gradient rather than the per-example one. Backpropagation through
a mean loss delivers $d/n$, so an implementation must undo the batch mean --- the reference
implementation this file was transcribed from writes \texttt{z\_grads[k] * B} with exactly that
comment, and the transcription dropped it while the call site divided by $n$ a second time. The
consequence is silent and severe. With $G$ smaller by $n^2 = 10^4$ and the damping a fixed
constant, the effective damping is $10^2$ too large, both Kronecker factors are replaced by
multiples of the identity, and the rule degenerates into rescaled gradient descent: measured, its
step sits at $\cos = 0.890$ to the plain gradient step, against $0.159$ once corrected.

Two conclusions we drew from the faulty runs are therefore withdrawn. K-FAC's accuracy of $0.615$
on ReLU is not K-FAC's. And its $\ker M$ fraction of $0.013$, sitting beside gradient descent's
$0.0000$, cannot be read as confirming the framework's prediction that a two-sided preconditioner
acts on the operator's geometry: gradient descent's step is $M^\top\Delta_\star$, which lies in
$\mathrm{range}(M^\top)$ by construction, so a rule that is $89\%$ gradient descent inherits a low
$\ker M$ fraction for free. The prediction about K-FAC is untested, not confirmed.

The learning-rate grid moves with the fix. Under the faulty scaling every rate from $10^{-2}$ to
$3\cdot10^{-1}$ diverged, which we read at the time as K-FAC being unstable at this depth;
corrected, nothing in that range diverges at all and the rule fits at $3\cdot10^{-3}$ and
$10^{-2}$. Both readings of the original grid were wrong for the same reason.

\emph{A second, earlier implementation fault in the solve.}
K-FAC's higher learning rates were failing with \texttt{torch.linalg.solve: singular matrix} a few
seconds into each run. That is not divergence and we initially misread it as such. The Kronecker
factors here are singular \emph{by construction}: $A_k = \frac{1}{n}h_{k-1}^\top h_{k-1}$ and
$G_k = \frac{1}{n}d_k^\top d_k$ are formed from a batch of $n = 100$ samples at width $128$, so
their rank is at most $100 < 128$, and the solve is well posed only because of the damping. The
factored Tikhonov split multiplies one factor's damping by $\pi$ and divides the other's by it, so
when $\pi$ drifts far from $1$ one factor is left effectively undamped and the solve fails. The fix
clamps $\pi$ and floors each factor's damping relative to that factor's own scale, and is covered
by a regression test that drives $\pi$ to both extremes across sixteen orders of magnitude of
residual scaling. Every K-FAC cell was re-run under the corrected solver, including those that had
completed under the old one, so that no row of this table mixes two implementations.

\paragraph{The $\ker M$ fraction separates the eight optimisers into two groups with nothing
in between.} $\ker M$ is the set of weight directions that leave the operator unchanged, so the
fraction of a step lying in it is the part of that step which provably does nothing to the function
at the point where it is taken. Measured at each optimiser's best fitting cell, at its
convergence-phase snapshot, three seeds:

\begin{center}\small
\begin{tabular}{lcc|lcc}
\toprule
\multicolumn{3}{c|}{step stays in $\mathrm{range}(M^\top)$} & \multicolumn{3}{c}{step leaves it} \\
optimiser & ReLU & CReLU & optimiser & ReLU & CReLU \\
\midrule
gradient descent & $0.0000$ & $0.0000$ & Muon    & $0.371$ & $0.529$ \\
reference        & $0.0000$ & $0.0000$ & SOAP    & $0.406$ & $0.480$ \\
heavy ball       & $0.0002$ & $0.0000$ & Adam    & $0.512$ & $0.688$ \\
K-FAC            & $0.015$  & $0.046$  & Shampoo & $0.570$ & $0.646$ \\
\bottomrule
\end{tabular}
\end{center}

\noindent The gap is the striking part: no optimiser measures between $0.046$ and $0.371$. Two of
the left-hand entries are zero \emph{by construction} --- gradient descent's step is
$M^\top\Delta_\star$ and the reference's is $M^{+}\Delta_\star$, both in $\mathrm{range}(M^\top)$
--- so those columns calibrate the projection rather than reporting a property of the rule, and
they read $0.0000$. For gradient descent this recovers, as a measurement on gated networks, the
conservation law that gradient flow on homogeneous factored models does not move along the
layer-rescaling symmetries \citep{du2018algorithmic}. The new part is that heavy ball and K-FAC
join them, and that the remaining four methods put between a third and two thirds of every step
into directions the function cannot see.

\paragraph{The grouping is not yet tested, and refutes any reading of it as an explanation of
generalisation.} K-FAC is the comparator the framework made a specific prediction about, and its
row is withheld pending the re-run: under the faulty scaling it read $0.013$, but a rule that is
$89\%$ gradient descent would read low whatever its geometry, so that number is not evidence
either way. Muon, SOAP and Shampoo all land with Adam.

The two groups do not sort by accuracy. Their means are $0.572$ and $0.520$, and each contains both
the best and among the worst methods in the table --- heavy ball at $0.642$ and gradient descent at
$0.504$ on the left, Adam at $0.634$ and SOAP at $0.458$ on the right. Over the sixteen
(architecture, optimiser) points the correlation between $\ker M$ fraction and test accuracy is
$-0.33$, and $-0.38$ and $-0.29$ within the two architectures: weak, and of the opposite sign to
the one the three-arm table of E4 suggests. We record that explicitly because the three-arm
ordering --- gradient descent $0.000$ at $0.53$, reference $0.001$ at $0.56$, Adam $0.51$ at
$0.63$ --- is monotone, and with three points it invites a causal reading that five further
optimisers do not support.

So the diagnostic does what a diagnostic should: it identifies, sharply and reproducibly, what kind
of object an optimiser's step is with respect to the parameterisation's symmetries. It does not
say how well that optimiser generalises, and Section~\ref{sec:e18b} reaches the same verdict about
$\cos_{op}$ from a different direction.

% ---------------------------------------------------------------------------
\subsubsection{E18b --- Which diagnostics actually track accuracy}
\label{sec:e18b}

\paragraph{Motivation and question.}
Part II reports nine diagnostics and one accuracy, arm by arm. With eight optimisers now in the
same table it becomes possible to ask the question the arm-by-arm presentation cannot:
\emph{across all of them, does any diagnostic predict generalisation?} The per-arm tables invite
the eye to find patterns in eight points, which is not enough to distinguish a property of the
measure from a property of the optimiser that happened to produce it.

\paragraph{Setup and measurement.}
The unit is a \emph{cell} --- one (architecture, optimiser, learning rate) triple, averaged over
its seeds --- not an arm at its best setting, which gives $49$ fitting cells across $16$
(architecture, optimiser) groups. Every diagnostic and the accuracy are read at the convergence
phase. Cells that never reach training accuracy $0.999$ are excluded, since a diagnostic measured
on a network that has not fitted is not comparable to one measured on a network that has.

We report two correlations with test accuracy, and the distinction between them is the point.
\emph{Across cells} is the ordinary correlation over all $49$. \emph{Within optimiser}
residualises both the diagnostic and the accuracy on a full set of (architecture, optimiser)
indicators first, so it asks: holding the optimiser fixed, does moving this diagnostic move
accuracy? A measure can score high across cells purely by identifying which optimiser produced the
cell. We also refit each within-optimiser correlation $16$ times, dropping one group each time,
because with this many mutually correlated diagnostics a figure that rests on a single optimiser
is not a finding.

\paragraph{Status.} \textsc{done} for the seven optimisers that have a fitting cell; Shampoo's
CReLU cells are still arriving and will add one more group.

\paragraph{Results.}

\begin{center}\small
\begin{tabular}{lccc}
\toprule
diagnostic & across cells & within optimiser & dropping any one optimiser \\
\midrule
gate diversity        & $+0.27$ & $\mathbf{+0.91}$ & $+0.69$ to $+0.94$ \\
training loss (log)   & $-0.57$ & $-0.52$ & $-0.58$ to $-0.49$ \\
\midrule
reachability $\alpha$ & $-0.15$ & $+0.86$ & $+0.06$ to $+0.90$ \\
operator rank         & $-0.32$ & $+0.65$ & $-0.14$ to $+0.74$ \\
gate density          & $-0.60$ & $+0.53$ & $-0.66$ to $+0.64$ \\
$\cos_{op}$           & $+0.30$ & $+0.41$ & $+0.14$ to $+0.72$ \\
gate churn            & $+0.48$ & $+0.32$ & $+0.25$ to $+0.55$ \\
$\cos_w$              & $+0.23$ & $+0.32$ & $-0.10$ to $+0.65$ \\
dead units            & $+0.15$ & $+0.07$ & $-0.35$ to $+0.18$ \\
\bottomrule
\end{tabular}
\end{center}

\noindent The horizontal rule is the finding. Only the top two rows have a within-optimiser
relationship that survives removing any single optimiser; everything below it, including three
measures whose headline figure looks strong, collapses or changes sign when one group is dropped.
Reachability $\alpha$ reads $+0.86$ over all cells and $+0.06$ without one of them. Operator rank
and gate density both cross zero. These are not weak correlations that need more data --- they are
correlations carried by one optimiser each.

\paragraph{Conclusions.}
\emph{Gate diversity is the only diagnostic here that reliably tracks accuracy within an
optimiser}, at $+0.91$ with a worst case of $+0.69$ across the sixteen refits, and it is also
$+0.90$ after additionally controlling for $\log$ learning rate. How differently the gate pattern
responds to different inputs orders the settings of every optimiser we measured. This is the
quantitative form of E8's finding, and it is the one structural measure in this paper we would be
willing to defend as a general relationship.

\emph{$\cos_{op}$ is a weak predictor, not a null one, and we have corrected this.} An earlier
version of this analysis used $33$ cells across $11$ groups and reported $+0.12$, which we
described as no relationship. With the full grid it is $+0.41$, stable in sign but ranging from
$+0.14$ to $+0.72$ depending on which optimiser is excluded. The honest statement is narrower than
the one we first drew: operator-space faithfulness does carry some within-optimiser information
about generalisation, and is clearly below gate diversity, and cannot be claimed as \emph{the}
quantity that explains it. The framework's central negative result --- that the most faithful rule
is not the best-generalising one --- rests on E5 and E18, not on this correlation.

\emph{Across and within can disagree badly, and a table of arms-at-their-best cannot tell you
which you are reading.} Gate density correlates $-0.60$ with accuracy across cells and $+0.53$
within optimiser; gate diversity is $+0.27$ across and $+0.91$ within. In the first case the sign
reverses, in the second the magnitude more than triples. Any statement of the form ``low $X$
accompanies good generalisation'' drawn from a comparison of optimisers at their best settings is
exposed to both, and several such statements in earlier drafts of this paper were wrong for
exactly this reason.

\emph{What this cannot support.} Forty-nine cells, sixteen groups and nine mutually correlated
diagnostics cannot establish that gate diversity is causal, and the leave-one-out column is a
reason for caution about the rest rather than a licence to trust the top two rows completely. What
the analysis does establish is procedural: a within-optimiser check and a leave-one-out refit are
cheap, and between them they disqualify five of the nine measures that a single pooled correlation
would have reported as informative.

% ---------------------------------------------------------------------------
\subsubsection{E19 --- A scaling check on CIFAR-10}
\label{sec:e19}

\paragraph{Motivation and question.}
Everything above is on one small task. \emph{Do the diagnostics remain computable, and the ordering
recognisable, at a scale where the step map has two orders of magnitude more equations?}

\paragraph{Setup.}
CIFAR-10, $45000$ training, $5000$ validation, $10000$ test, depth $8$, width $128$, $20000$ steps
($\approx 44$ epochs at batch $100$). The alignment probe runs in float32 with batch $32$ and
$k = 1200$; at this scale the step map has $3.07$M equations against $40$k for MNIST-1D, so the
float64 probe used in Part II is not affordable and the reduced budget is recorded through the
solve-quality statistic rather than assumed harmless.

\paragraph{Status.} \textsc{partial}. The baseline grid is complete at three seeds on both
architectures; the reference arm has one cell and the rest of its grid is running.

\paragraph{Results.} Every completed cell, three seeds except where noted. Scored at the end of the
budget, because \emph{no cell has a convergence phase} --- the highest training accuracy anywhere
in this table is $0.928$:

\begin{center}\small
\begin{tabular}{llccc}
\toprule
architecture & arm & lr / $\eta$ & test acc. & max train acc. \\
\midrule
ReLU  & gradient descent & $0.01$  & $0.464$ & $0.859$ \\
      &                  & $0.03$  & $0.473$ & $0.855$ \\
      &                  & $0.1$   & $0.488$ & $0.806$ \\
      & Adam             & $0.001$ & $0.491$ & $0.861$ \\
      &                  & $0.003$ & $\mathbf{0.501}$ & $0.775$ \\
      & reference        & $0.3$   & $0.441$ & $0.658$ \ \small(one seed) \\
\midrule
CReLU & gradient descent & $0.01$  & $0.469$ & $\mathbf{0.928}$ \\
      &                  & $0.03$  & $0.480$ & $0.875$ \\
      & Adam             & $0.001$ & $\mathbf{0.505}$ & $0.896$ \\
      &                  & $0.003$ & $0.445$ & $0.746$ \\
\bottomrule
\end{tabular}
\end{center}

\paragraph{Interim conclusions and an important caveat.}
The diagnostics remain computable at this scale, which was the primary question. The accuracy
ordering is \emph{not} the one seen on MNIST-1D, but the comparison is not a fair one, and the
reason is now quantified rather than asserted: \emph{no arm interpolates here}. The best training
accuracy in the whole table is $0.928$, and the convergence-phase estimator that scores every other
experiment in Part II returns nothing for every single cell. This compares arms at matched
\emph{budget} rather than at matched \emph{fit} --- exactly the confound E5 was designed to remove,
and E12's width-$16$ row shows what that confound does to a conclusion. The operator step costs
roughly forty times a gradient step at this scale, so equalising fit requires either a much longer
run for the reference or a smaller configuration.

We therefore report this as a feasibility check and not as evidence about generalisation, and we
would not include the accuracy column in a final version without either equalising fit or stating
the budget mismatch as prominently as it is stated here.

% ---------------------------------------------------------------------------
\subsubsection{E20 --- Noise in the step, and where it is allowed to live}
\label{sec:e20}

\paragraph{Motivation and question.}
Two observations from the experiments above point the same way. First, the reference generalises
\emph{better at larger target size}: at the convergence phase on ReLU its accuracy rises
monotonically with $\eta$, from $0.505$ at $\eta = 0.1$ to $0.565$ at $\eta = 1$, and the same
ordering holds on CReLU ($0.540 \to 0.592$) and leaky ($0.519 \to 0.567$). Second, larger $\eta$
crosses more nonlinearities: gate churn over the same cells rises monotonically from $0.0141$ to
$0.0423$. A step large enough to move pre-activations across zero changes which linear region the
model is in, which injects a form of stochasticity into the trajectory that a small step does not
have. Both trends are read at matched phase, so neither is an artefact of the larger-$\eta$ cells
spending longer past interpolation.

That suggests a mechanism rather than an accident, and it is one with a long literature behind it.
Noise in the update is a classical route to flat minima --- regions of weight space over which the
error is approximately constant \citep{hochreiter1997flat} --- whether it arises from Langevin
dynamics \citep{welling2011sgld}, from explicit gradient-noise injection
\citep{neelakantan2015adding}, from the minibatch noise of SGD itself
\citep{smith2020generalization}, or from deliberately anticorrelated perturbations
\citep{orvieto2022anticorrelated}.

\emph{So: does injecting noise into the reference step improve its generalisation, and if it does,
is the benefit coming from perturbing the function or from relocating the weights?} The second half
is the part this framework can answer and isotropic noise injection cannot.

\paragraph{The decomposition, and why it is the interesting measurement.}
Weight space splits as $\mathrm{range}(M^\top) \oplus \ker M$. A perturbation in $\ker M$ moves the
weights while leaving $M(\Delta W) = 0$: to first order the operator, and therefore the function the
network computes, is \emph{unchanged}. That is exactly Hochreiter and Schmidhuber's flat region,
identified constructively rather than searched for. A perturbation in $\mathrm{range}(M^\top)$ is
the complement and changes the operator.

This gives three arms that inject the same amount of noise in different places:
\begin{equation}
  \Delta W \;=\; \Delta W^{\mathrm{ref}} \;+\;
  \varepsilon\,\frac{\|\Delta W^{\mathrm{ref}}\|}{\|\Pi_\bullet g\|}\,\Pi_\bullet g ,
  \qquad g \sim \mathcal N(0, I),
  \label{eq:opnoise}
\end{equation}
with $\Pi_\bullet$ the identity (\textsc{iso}), the projection onto $\ker M$ (\textsc{ker}), or the
projection onto $\mathrm{range}(M^\top)$ (\textsc{range}). Scaling by
$\|\Delta W^{\mathrm{ref}}\|$ makes $\varepsilon$ dimensionless and comparable across $\eta$.

The three are separated as intended. Measured at $\varepsilon = 0.1$ on ReLU, the operator
perturbation induced relative to the reference step's own operator motion is $0.137$ for
\textsc{iso}, $0.017$ for \textsc{ker} and $0.324$ for \textsc{range}: the \textsc{ker} arm moves
the weights by the same amount while moving the function by an order of magnitude less.

\paragraph{How large is $\ker M$, and what that implies for the design.}
The three arms are only three different experiments if the projections actually move the noise
somewhere it would not otherwise be. Measuring the fraction of a random direction that already
lies in $\ker M$:

\begin{center}\small
\begin{tabular}{lccc}
\toprule
fraction of a random $g$ in $\ker M$ & $L=2$ & $L=8$ & $L=32$ \\
\midrule
ReLU  & $0.02$ & $0.63$ & $0.94$ \\
CReLU & $0.13$ & $0.81$ & $0.96$ \\
\bottomrule
\end{tabular}
\end{center}

The null space grows quickly with depth, and this reshapes the experiment. At $L=8$ isotropic
noise is \emph{already} $63$--$81\%$ function-preserving, so the \textsc{ker} arm is a refinement
of \textsc{iso} rather than a contrast with it, and the two may be hard to tell apart. The arm
that genuinely differs is \textsc{range}, which concentrates all of the perturbation into the
minority of directions that change the operator. At $L=32$ the point is sharper still: isotropic
noise there is $94$--$96\%$ confined to $\ker M$, so the cheap arm is essentially the
function-preserving experiment already, and only the \textsc{range} projection has to be paid for.

This is not a detail of implementation. It says that in a deep network, most of what any noise
injection method does is move weights without changing the function --- and that the flat-minima
reading of noise injection is, in these models, a statement about the majority of the perturbation
rather than an incidental part of it.

\paragraph{Setup.}
MNIST-1D, the common configuration, ReLU, $\eta = 1$, $k = 50$,
$\varepsilon \in \{0.01, 0.03, 0.1, 0.3\}$, two seeds, with $\varepsilon = 0$ rerun inside the same
sweep as the control. The \textsc{ker} and \textsc{range} arms require a projection at every step
and are correspondingly more expensive, so they follow the isotropic pilot.

\paragraph{Measurement.}
Test accuracy against $\varepsilon$ at the convergence phase, paired to the $\varepsilon = 0$
control \emph{seed by seed}; training loss and accuracy over the same window, to establish whether
any change is a generalisation effect or a fitting one; and gate churn, to test whether injected
noise reproduces the gate-crossing behaviour that larger $\eta$ produces.

\paragraph{Status.} \textsc{partial}. The isotropic sweep is complete at two seeds and
\textsc{range} at $\varepsilon = 0.1$; the rest of \textsc{range} is running and \textsc{ker} is
not yet started.

\paragraph{Results.} Isotropic noise, ReLU, $\eta = 1$, $k = 50$, two seeds, paired against the
$\varepsilon = 0$ control run inside the same sweep. Both windows are shown, because they disagree
and the disagreement is the finding:

\begin{center}\small
\begin{tabular}{lccccccc}
\toprule
$\varepsilon$ & $0$ & $0.01$ & $0.03$ & $0.1$ & $0.3$ & $1$ & $3$ \\
\midrule
test acc.\ (convergence)  & $0.560$ & $0.559$ & $\mathbf{0.572}$ & $0.565$ & $\mathbf{0.573}$ & $0.556$ & --- \\
paired $\Delta$           & ---     & $-0.001$ & $+0.012$ & $+0.005$ & $+0.013$ & $-0.004$ & --- \\
\midrule
test acc.\ (endpoint)     & $0.560$ & $0.568$ & $0.575$ & $\mathbf{0.577}$ & $0.566$ & $0.559$ & $0.455$ \\
paired $\Delta$           & ---     & $+0.008$ & $+0.015$ & $\mathbf{+0.017}$ & $+0.005$ & $-0.000$ & $-0.104$ \\
\midrule
train loss (conv.)        & $5.6\cdot10^{-4}$ & $2.6\cdot10^{-3}$ & $5.0\cdot10^{-4}$
                          & $8.5\cdot10^{-4}$ & $1.1\cdot10^{-3}$ & $7.8\cdot10^{-4}$ & --- \\
train acc.\ (conv.)       & $1.000$ & $0.999$ & $1.000$ & $1.000$ & $1.000$ & $1.000$ & never \\
step at convergence       & $1625$ & $1500$ & $1875$ & $1625$ & $1625$ & $2625$ & --- \\
gate churn (conv.)        & $0.0461$ & $0.0478$ & $0.0424$ & $0.0474$ & $0.0483$ & $0.0411$ & --- \\
\bottomrule
\end{tabular}
\end{center}

\noindent The two per-seed differences behind each convergence-phase $\Delta$, in order, are
$(+0.001, -0.003)$, $(+0.014, +0.011)$, $(+0.004, +0.006)$, $(+0.019, +0.007)$ and
$(-0.020, +0.011)$ --- so the sign agrees across both seeds everywhere except at the two ends of
the sweep. The $\varepsilon = 3$ column has no convergence phase: it never reaches training
accuracy $0.999$, so it is a fitting failure rather than a generalisation result and only its
endpoint is reportable.

\paragraph{Conclusions so far.}
\emph{The effect is real but roughly half the size we first reported, and the difference is the
window.} Read at the end of the budget, the gain peaks at $+0.017$ at $\varepsilon = 0.1$ with a
clean single-peaked shape. Read at the convergence phase, it is $+0.012$ at $\varepsilon = 0.03$ and
$+0.013$ at $\varepsilon = 0.3$, with $\varepsilon = 0.1$ falling to $+0.005$ and the shape no
longer single-peaked. The endpoint reading was flattering the middle of the sweep: noise reaches
interpolation at a similar step but then holds up better over the long post-interpolation tail in
which the unperturbed reference degrades (Remark~\ref{rem:normalise}), so part of what the endpoint
scored as a generalisation gain was a slower decay after learning had finished. What survives at
matched phase is smaller and less regular --- positive in six of eight per-seed comparisons, and in
four of four at $\varepsilon \in \{0.03, 0.1\}$ --- but it is positive, at a magnitude of about half
the reference's margin over gradient descent at this depth rather than all of it. With two seeds we
report it as suggestive and not as established.

\emph{The mechanism we proposed is refuted.} The motivation for this experiment was that larger
$\eta$ both generalises better and crosses more nonlinearities, suggesting the gate crossings were
doing the work. Injected noise does not reproduce that at any magnitude: gate churn over the
convergence window runs $0.0411$--$0.0483$ across the entire sweep with no trend in $\varepsilon$,
and the same flatness holds at the endpoint. So noise improves generalisation here \emph{without}
increasing gate crossing, and the link between the $\eta$ trend and the crossings is not the
explanation --- which leaves the $\eta$ trend itself unexplained and makes the isotropic gain harder
to attribute, not easier.

\emph{The \textsc{range} arm, so far, is indistinguishable from \textsc{iso}.} At
$\varepsilon = 0.1$ it gives $+0.005$ at the convergence phase against \textsc{iso}'s $+0.005$
(per seed $+0.008, +0.003$ against $+0.004, +0.006$). With one $\varepsilon$ and two seeds this
separates nothing, and the remaining cells are running.

That sharpens what the projected arms are for. If the benefit were about traversing the partition
it should have shown up in churn and did not, so the remaining candidates are that the noise
relocates the weights within a level set --- which \textsc{ker} tests directly --- or that it
perturbs the function itself, which \textsc{range} isolates.

\subsubsection{Summary: what separates what}
\label{sec:e-summary}

Collecting every measure in Part II, with the direction of the effect and what it distinguishes.
Each column is the arm at its best hyper-parameter by the convergence-phase estimator, read over
the same window, with the range spanning the three gated architectures.

\begin{center}\small
\begin{tabular}{lccc@{\hskip 10pt}l}
\toprule
measure & gradient descent & Adam & reference & separates \\
\midrule
$\cos_{op}$ (E4)            & $0.20$--$0.25$   & $0.03$--$0.07$     & $0.41$--$0.94$     & all three, inversely to accuracy \\
$\cos_w$ (E4)               & $0.12$--$0.27$   & $-0.00$--$0.07$    & $0.21$--$0.57$     & all three \\
reachability $\alpha$ (E3)  & $1.000$          & $\ge 0.87$         & $0.99$--$1.00$     & nothing \\
$\ker M$ fraction (E4)      & $0.0000$         & $0.56$--$0.81$     & $0.0000$           & rules with memory \\
operator rank (E9)          & $5.7$--$6.6$     & $2.0$--$3.9$       & $6.1$--$7.0$       & Adam only; as diversity \\
operator diversity (E9)     & $80$--$86\%$     & $36$--$83\%$       & $65$--$96\%$       & Adam only; as rank \\
switching fraction (E8)     & $0.996$--$0.999$ & $0.92$--$1.00$     & $0.76$--$0.79$     & the reference from both \\
region concentration (E11)  & baseline         & $-0.02$--$+0.02$   & $+0.08$--$+0.13$   & the reference from both \\
CW-PGD$_{0.05}$ / clean (E16) & $57$--$59\%$   & $47$--$66\%$       & $63$--$66\%$       & nothing \\
\midrule
test accuracy (E5)          & $0.50$--$0.54$   & $0.61$--$0.65$     & $0.56$--$0.59$     & --- \\
\bottomrule
\end{tabular}
\end{center}

Two groups emerge. Switching separates the \emph{reference from both baselines} and does so in both
windows. Operator rank and diversity, which agree with each other, separate \emph{Adam from the
other two}: Adam is lowest on both in all three architectures, while the reference and gradient
descent swap places between architectures and sit close together. That is a weaker separation than
the endpoint table appeared to give --- where all three were ordered --- and the direction that
survives is the one that matters least for the framework's thesis, since it says only that the
\emph{least} operator-faithful rule arrives at the simplest operator while generalising best.

Neither grouping matches the accuracy ordering. Nothing here explains the gradient-descent--Adam
gap, which is the largest of the three: on region concentration the two differ by $|t| \le 2.6$
with the sign reversing between architectures, on switching they differ in the wrong direction, and on
rank and diversity they differ in the right direction but the reference --- which sits between them
in accuracy --- is at the \emph{opposite} end from Adam rather than between them.

$\cos_{op}$ is the only measure that cleanly separates all three, and it orders them \emph{inversely}
to accuracy. Section~\ref{sec:e18b} qualifies this by adding five more optimisers and changing the
unit of analysis from an arm to a cell: over $49$ fitting cells, $\cos_{op}$ has the widest range
of any diagnostic in this table and a within-optimiser relationship with accuracy of $+0.41$,
below gate diversity's $+0.91$ and the only one of the two that does not survive dropping an
optimiser. The measure separates optimisers and orders the settings of any one of them only
weakly. E15 shows that this inversion must not be read as a causal claim, and why: the
diagnostic compares single steps, so a rule with memory scores low for a reason unrelated to the
quality of its direction. Adam carries a first moment; operator momentum reproduces its low
$\cos_{op}$ without reproducing any of its benefit, and adding accumulated operator motion to the
reference rule does not recover Adam's advantage. So the framework's central diagnostic is a genuine
and reproducible ordering of optimisers that does \emph{not} reduce to generalisation, and one whose
value for momentum-carrying rules is a statement about their steps rather than their trajectories.

Five cautions on reading the table. Operator rank and robustness vary more \emph{within} an arm
across its hyper-parameter grid than \emph{between} arms at their best settings, so a per-arm row
understates the variation. The reference's $\cos_{op}$ row is partly a statement about Krylov
truncation at $k = 50$ rather than about the rule, as E4 and E17 discuss. Adam's $\alpha$ entry is
a lower bound from a budget-limited solve (Section~\ref{sec:e3}), which is why it is written as an
inequality. The switching and diversity rows are comparisons at a fixed probe size and would move
with it, though not in their ordering. And the $\ker M$ row is the one quantity here with a known
ground truth --- two of its three entries are zero by construction --- which is what makes it
usable as a calibration of the projection that measures it, rather than only as a comparison. That
row separates rules that accumulate a direction from rules that do not, rather than adaptive from
non-adaptive ones, and Section~\ref{sec:e18} shows it does not order optimisers by accuracy.

A seventh caution, from Section~\ref{sec:e18b}: every row of this table is an arm at its best
setting, and at least one measure --- gate density --- correlates with accuracy in \emph{opposite
directions} between cells of one optimiser and cells across optimisers. A row read as ``low $X$
accompanies good generalisation'' can therefore be true of this table and false of every individual
optimiser in it.

A sixth caution deserves its own sentence, because it is the one that changed most in preparing
this table. Every row is now read at the convergence phase, and three of them --- operator rank,
operator diversity, and the layer-wise compression of Section~\ref{sec:e10} --- \emph{reverse}
between that window and the end of the budget. The reversal is confined to the reference arm, is
explained by the normalisation of Remark~\ref{rem:normalise}, and does not affect the accuracy row
by more than $0.021$. But it means that a structural claim about this rule is only as good as the
window it was read in, and the window has to be stated.

\section{Conclusion}
\label{sec:conclusion}

Implicit bias is usually studied through the solutions an optimiser reaches. We have argued that it
can be studied through the steps it takes, provided one is willing to compute, at each point, the
weight update that would have realised what the loss asked of the operator. That reference step is
available in closed form for deep linear chains, where it is exact even at parameter values that
stop every derivative-based method, and as a least-squares solve for gated networks, where the
request is no longer fully realisable and the shortfall is itself a measurable property of the
architecture.

The resulting decomposition separates two things that endpoint studies necessarily conflate: what
the architecture permits, and what the optimiser does within what is permitted. The first is
optimiser-independent; the second is scale-invariant, and therefore free of the tuning objections
that comparisons between optimisers usually attract.

\emph{[Summary of findings to be inserted once the measurements of
Sections~\ref{sec:linear-exp} and \ref{sec:nonlinear-exp} are complete.]}

\paragraph{Limitations.}
The reference step is defined by a first-order model of how weights move the operator, and is
therefore meaningful only for steps small enough to stay in that regime; we report the step sizes
at which this holds. The least-squares solve becomes ill-conditioned with depth, and the
weight-space diagnostic $\cos_{\mathrm{w}}$ in particular is reliable only where the solve attains
the minimum-norm solution, which we verify per measurement rather than assume. Finally,
Remark~\ref{rem:fidelity} bears repeating: the diagnostics measure fidelity to the operator
gradient, which is not the same as optimisation quality, and nothing here should be read as a claim
that the reference step is a better training algorithm.

\newpage
\subsection*{AI use statement}

(This section is \textbf{required} and does not count toward the page limit.)

In this work, we used generative AI tools for [tasks with required disclosure].
We have not used generative AI tools for [other tasks with required disclosure],
and [the rest of the required disclosure tasks] are not applicable to this work.
Additionally, we used generative AI tools for [tasks with recommended
disclosure]. We have reviewed all AI-assisted work. [Elaborate. For example, “we
checked LLM-generated research ideas for potential plagiarism through a manual
literature survey”, “LLM-generated code was verified and tested for correctness
by 2 authors”, etc.]. We take responsibility for the final content of this work,
including text, claims or artifacts produced with the aid of generative AI.

See the ICLR 2027 AI Policy for Authors for more details. This statement should
not be more than 1 page.

\subsection*{Ethics statement}

\subsection*{Reproducibility statement}

\subsubsection*{Author Contributions}

\subsubsection*{Acknowledgments}

\bibliography{references}
\bibliographystyle{iclr2027_conference}

\appendix
\section{Appendix}

\section{Proofs}
\label{app:proofs}

\emph{[No proofs are currently carried here; the structural identities used in the text are
verified numerically in Section~\ref{sec:diagnostics} and its experiments.]}

\section{Training dynamics on the remaining architectures}
\label{app:dynamics}

Figure~\ref{fig:dynamics} in the main text shows the full trajectories on ReLU. The same figure
for the other two gated architectures follows. The features named there hold in both: the arms
interpolate at different times, test loss rises for every arm while test accuracy does not fall,
gradient descent is static once it has fitted, and the operator arm's rank declines after it has.
The CReLU panels also show the seed spread that makes the CReLU Adam cell the widest in Part II
--- its band in test accuracy is several times any other arm's, which is the $\pm 0.071$ reported
in Section~\ref{sec:e5}.

\begin{figure}[H]
\centering
\includegraphics[width=\textwidth]{dynamics_crelu.pdf}
\caption{As Figure~\ref{fig:dynamics}, on CReLU.}
\label{fig:dynamics-crelu}
\end{figure}

\begin{figure}[H]
\centering
\includegraphics[width=\textwidth]{dynamics_leaky.pdf}
\caption{As Figure~\ref{fig:dynamics}, on leaky ReLU ($\alpha = 0.1$), three seeds.}
\label{fig:dynamics-leaky}
\end{figure}

\section{Numerical behaviour of the least-squares solve}
\label{app:numerics}

\emph{[Divergence of LSQR past convergence on these step maps; failure of the two natural stopping
tests; the normal-equation stopping rule and the attained residuals by depth and architecture.]}

% =========================================================================
\section{Hardware, cost, and what each method costs}
\label{app:cost}

\subsection{Hardware and software}

All experiments ran on a single workstation: one NVIDIA RTX 6000 Ada Generation
($47.4$\,GiB, $142$ SMs, compute capability $8.9$), an Intel Core i9-14900 ($32$ threads) and
$123$\,GiB of host memory, under Linux 6.17, CUDA 13.0, PyTorch 2.11 and Python 3.13. No
distributed or multi-GPU training was used anywhere, and no experiment in this paper requires it.

One property of this device shapes several design decisions below. Measured on $2048^2$ matrix
products, it sustains $36.1$ TFLOP/s in float32 and $0.99$ TFLOP/s in float64, a ratio of
$37{:}1$. Every diagnostic that has to be trusted to many digits --- the reachability check
$\alpha \le 1$, the normal-equation residual, the structural identities --- runs in float64 and
therefore runs at roughly one thirty-seventh of peak. This is affordable at MNIST-1D scale and is
not at CIFAR-10 scale, which is why the probe there runs in float32 with its solve quality
recorded rather than assumed (Section~\ref{sec:e19}).

\subsection{Cost of one optimiser step}

Two different questions get two different answers, and conflating them is the usual way cost
comparisons mislead.

\paragraph{The rule alone.} Milliseconds per optimiser step, measured with every
(method, depth) pair warmed before any timing, so that no measurement absorbs the one-time
workspace allocation for a shape it happens to touch first:

\begin{center}\small
\begin{tabular}{lcccc}
\toprule
method & $L=2$ & $L=8$ & $L=32$ & $\times$ gd at $L=8$ \\
\midrule
gradient descent      & $0.1$  & $0.5$   & $2.1$   & $1\times$ \\
Adam                  & $0.2$  & $0.7$   & $2.9$   & $1\times$ \\
heavy ball            & $0.1$  & $0.6$   & $2.5$   & $1\times$ \\
Muon                  & $0.5$  & $1.9$   & $7.5$   & $4\times$ \\
K-FAC                 & $3.9$  & $16.6$  & $67.5$  & $31\times$ \\
SOAP                  & $4.3$  & $17.3$  & $64.1$  & $33\times$ \\
Shampoo               & $41.3$ & $163.9$ & $617.7$ & $312\times$ \\
\midrule
reference ($k=50$)    & $66.1$ & $76.1$  & $114.5$ & $145\times$ \\
\; + momentum (E15)   & $66.7$ & $76.2$  & $115.2$ & $145\times$ \\
\; + isotropic noise (E20) & $66.2$ & $76.2$ & $114.7$ & $145\times$ \\
\; + $\ker M$ noise (E20)  & $493.1$ & $814.1$ & $982.6$ & $1549\times$ \\
\bottomrule
\end{tabular}
\end{center}

\paragraph{How this scales, which is the part that matters.} The reference's relative cost
\emph{falls sharply with depth}: $661\times$ gradient descent at $L=2$, $152\times$ at $L=8$,
$55\times$ at $L=32$. The reason is that the step solves $M^{+}\Delta_\star$ by $k$ Krylov
iterations, each of which is a handful of small kernels rather than one large one, so at these
sizes it is launch-bound rather than arithmetic-bound. Going from $L=2$ to $L=32$ multiplies the
parameter count by $78$ ($6.4$k to $498$k) and the time per step by only $1.7$. The overhead is
therefore worst on small models and amortises as the model grows --- the opposite of how a method
that explicitly inverted a curvature matrix behaves, and visible in the same table: Shampoo, which
does per-layer inverse roots at every step, costs $294\times$ gradient descent at $L=32$ against
the reference's $55\times$. At that depth the reference is the \emph{cheaper} of the two.

Momentum and isotropic noise are free on top of the reference: they add an elementwise operation
to a step already dominated by the solve, and drawing the noise itself is unmeasurable at
$0.0$\,ms. The projected arms are not free, and it is worth being exact about why, since "noise is
expensive" would be the wrong lesson. The cost is a \emph{second linear solve}, not the noise.
Measured at $L=8$: the reference step's own $50$ iterations cost $76$\,ms, or $1.52$\,ms each; the
projection's $300$ iterations cost $492$\,ms, or $1.64$\,ms each. The per-iteration cost is
essentially the same and the factor of six is entirely iteration count. Reducing the projection to
$150$ iterations halves it, at the price of leaking $3.0\%$ of the noise's operator motion back in
rather than $1.2\%$.

\paragraph{The run as a whole.} End-to-end wall clock per completed $12000$-step run, median over
every cell of that method and depth in the sweeps' own logs, including data loading, evaluation
probes, alignment probes, checkpointing and I/O. This is what each experiment actually cost, as
opposed to what its optimiser costs:

\begin{center}\small
\begin{tabular}{lccccccr}
\toprule
 & $L=2$ & $L=4$ & $L=8$ & $L=16$ & $L=32$ & $\times$ gd at $L=8$ & cells \\
\midrule
gradient descent   & $9$\,s   & $37$\,s  & $60$\,s  & $222$\,s & $229$\,s  & $1\times$ & $134$ \\
Adam               & $10$\,s  & $31$\,s  & $74$\,s  & $279$\,s & $417$\,s  & $1.2\times$ & $139$ \\
heavy ball         & ---      & ---      & $59$\,s  & ---      & ---       & $1.0\times$ & $15$ \\
Muon               & ---      & ---      & $75$\,s  & ---      & ---       & $1.2\times$ & $24$ \\
K-FAC              & ---      & ---      & $110$\,s & ---      & ---       & $1.8\times$ & $15$ \\
SOAP               & ---      & ---      & $112$\,s & ---      & ---       & $1.9\times$ & $29$ \\
Shampoo            & ---      & ---      & $429$\,s & ---      & ---       & $7.2\times$ & $24$ \\
\midrule
reference ($k=50$) & $111$\,s & $184$\,s & $320$\,s & $938$\,s & $1259$\,s & $\mathbf{5.3\times}$ & $87$ \\
\bottomrule
\end{tabular}
\end{center}

\noindent The comparators are run only at $L=8$, which is where E18 compares them, so their other
columns are empty by design rather than missing. Shampoo is the one comparator that costs more
end-to-end than the reference does --- $429$\,s against $320$\,s --- which is the practical form of
the scaling argument above.

The end-to-end ratio for the reference is $5.3\times$ where its per-step ratio is $152\times$, and
both are correct. A gradient-descent run spends $60$\,s on $12000$ steps that cost $0.5$\,ms each,
so over $90\%$ of its wall clock is evaluation, probing and I/O rather than optimisation. For every
method cheaper than about $5$\,ms per step the optimiser is simply not what one is paying for,
which is why Muon at $4\times$ gradient descent per step is indistinguishable from Adam
end-to-end. We report both figures because the per-step number is a property of the method and the
end-to-end number is a property of the experiment, and a reader deciding whether the reference is
affordable needs the second.

\paragraph{One discrepancy between the two tables, and what it means.} SOAP costs $17.3$\,ms per
step in the per-step benchmark but only $110$\,s end-to-end, where $17.3$\,ms $\times\,12000$ steps
alone would be $208$\,s. The benchmark is not wrong; it is measuring a different thing. SOAP
refreshes its preconditioner basis every $10$ steps, and a benchmark that times one step in a loop
pays that refresh on every step, whereas a real run pays it on one step in ten. The per-step table
therefore reports an upper bound for any method with an amortised update, and the end-to-end table
is the one to use for planning. K-FAC has the same structure and the same caveat applies to it.
The reference has no amortised component --- every step solves its own least-squares problem --- so
for it the two tables agree.

A final note in the method's favour that a wall-clock table cannot show: the reference has no
learning-rate grid to search. Its target size $\eta$ transfers across depth unchanged
(Section~\ref{sec:e2}), while the baselines' useful learning rates move by two to three orders of
magnitude between $L=2$ and $L=32$ and had to be swept at every depth. At equal \emph{tuning}
budget rather than equal wall clock, the comparison is considerably kinder to the reference than
the numbers above suggest.

\subsection{Cost of the diagnostics}

The alignment probe is the expensive measurement, not the optimiser. It solves the reference to
$k = 3000$ with damping, in float64, and its cost grows linearly with depth: $4.0$\,s at $L=8$,
$8.1$\,s at $L=16$, $16.5$\,s at $L=32$ per probe. At the default of one probe every $2000$ steps
this is a few percent of a run at $L=8$ and around a fifth of one at $L=32$.

At CIFAR-10 scale the same probe is not affordable in float64. The step map there has $3.07$M
equations against $40$k for MNIST-1D --- a $77\times$ increase, with every LSQR iteration growing
in proportion --- so the probe runs in float32 with batch $32$ and $k = 1200$, and the relative
normal-equation residual is recorded at every probe so that a reduced budget appears in the data as
a worse residual rather than as a silently wrong number.

\subsection{Cost of the experiments}

Total compute for every result in this paper, from each run's own recorded wall clock:

\begin{center}\small
\begin{tabular}{lrr}
\toprule
experiment & runs & GPU hours \\
\midrule
Main MNIST-1D grid (E3--E16) & $302$ & $25.0$ \\
CIFAR-10 (E19, partial) & $31$ & $17.6$ \\
Depth sweep (E17, partial) & $232$ & $4.9$ \\
Optimiser switch (E13) & $72$ & $2.8$ \\
Extended training (E14) & $4$ & $1.0$ \\
Two-dimensional toy (E2, E11) & $25$ & $0.2$ \\
Noise injection (E20, partial) & $2$ & $0.3$ \\
\midrule
total & $668$ & $\approx 53$ \\
\bottomrule
\end{tabular}
\end{center}

Roughly two GPU-days for the whole paper on one consumer-class card, with $2.5$\,GB of stored
metrics, checkpoints and snapshots. Two observations about where it goes. CIFAR-10 accounts for a
third of the total while contributing a single feasibility check, entirely because the operator
step costs about forty times a gradient step at that scale; and the depth sweep's remaining cells
at $L \in \{16, 32\}$ are projected at a further $17$ hours, which is more than everything
completed so far, since cost is linear in depth and those are the two deepest settings.

Every run is keyed by the SHA-1 of its configuration, so re-running any script costs nothing for
work already done and a new cell is simply added. That is what made it practical to extend grids
repeatedly rather than plan them correctly in advance.

\end{document}
